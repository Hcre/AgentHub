# === 根级环境变量（Docker Compose 读取）===

# --- PostgreSQL ---
POSTGRES_USER=agenthub
POSTGRES_PASSWORD=agenthub_dev_pwd
POSTGRES_DB=agenthub

# --- Redis ---
REDIS_PASSWORD=

# --- Backend ---
# AES-256-GCM 密钥（32 字节 base64），生成: python -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())"
SECRET_KEY=taHwC8oTGrdopDqI8P8jH//8l+HeOdxn5rYZexqAj7o=
JWT_SECRET=taHwC8oTGrdopDqI8P8jH//8l+HeOdxn5rYZexqAj7o=
# 默认 LLM Provider Key（可选；Agent 级别可单独配置）
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Adapter 模式：claude_cli | anthropic_api | mock（无 Key 时用 mock 跑通流程）
LLM_ADAPTER_MODE=mock

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
