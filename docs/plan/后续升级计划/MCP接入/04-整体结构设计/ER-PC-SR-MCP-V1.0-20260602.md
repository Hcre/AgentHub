# 演进路线 + 性能容量 + 结构风险（合并）— AgentHub MCP 接入 V1.0

> **项目代号**：MCP  | **版本**：V1.0  | **日期**：2026-06-02  | **棒位**：TD-001
> **合并说明**：本文档合并演进路线图（ER）+ 性能容量预判表（PC）+ 结构风险清单（SR）

---

# 第一部分：演进路线图（ER）

> 4 个演进阶段，全部含回滚策略 [R25]

## V1.0 MVP（当前交付，2026-06）

- **触发条件**：已就绪
- **新增模块**：M-001 ~ M-022（22 个）
- **废弃模块**：无
- **核心指标**：dry-run 误报率 10%、tool_call 99.5% SLO、50 并发/Agent

## V1.1 dry-run 误报率优化（2026-09，3 个月内）

| 项 | 内容 |
|---|---|
| 演进阶段 | V-001 |
| 触发条件 | 调研报告 R-008 给出路径 + CR-002 PM 拍板 |
| 新增模块/接口 | M-013 内新增 `SemgrepAssistantPlugin` + `ReachabilityAnalysisPlugin`（SandboxBackend SPI 扩展） |
| 废弃模块 | 无（兼容升级，不破坏旧实现） |
| 迁移策略 | 灰度（5% → 25% → 100%），按 mcp_id 哈希分流 |
| 回滚策略 | 灰度比例回退至 0% + 切回 V1.0 dry-run 算法 + 数据双写保障 15min 内可回退 |
| 风险等级 | 中（误报算法调整可能引入新误判，需对比基线） |
| 来源 | [SA:CR-002 + 调研报告 S-058 + R-008] |

## V1.2 用户自创模板审计（2026-12）

| 项 | 内容 |
|---|---|
| 演进阶段 | V-002 |
| 触发条件 | DE-018 用户自创模板量 > 10 / 月 OR 出现一次安全事件 |
| 新增模块/接口 | M-011 内新增 `StaticAnalysisGuard`（CVE-2025-49596 等规则强制） |
| 废弃模块 | 无 |
| 迁移策略 | 双写（DE-018 同时走旧路径与审计路径，对比 7 天） |
| 回滚策略 | 关闭静态分析开关 + 保留已有 DE-018 数据 + 5min 内可回退 |
| 风险等级 | 中（误判正常模板会阻塞 R-02 发布） |
| 来源 | [TD推断:CE-008 修复] |

## V1.3 跨平台沙箱统一（2027-Q1）

| 项 | 内容 |
|---|---|
| 演进阶段 | V-003 |
| 触发条件 | Windows Home 部署比例 > 5% OR CE-006 触发一次以上 |
| 新增模块/接口 | M-013 SandboxBackend SPI 新增 `gVisorPlugin` + `KataPlugin`；移除 setrlimit 后端 |
| 废弃模块/接口 | `SetrlimitBackend`（V1.3 起仅作为开发环境使用，生产环境强制阻断） |
| 迁移策略 | 蓝绿（新沙箱集群与旧并存 2 周，验证后切换） |
| 回滚策略 | DNS 切回旧集群 + 已运行的 dry-run 任务允许跑完 + 2h 内可回退 |
| 风险等级 | 高（沙箱后端切换可能影响所有 MCP 入库流程） |
| 来源 | [TD推断:CE-006 修复 + 调研报告 S-049] |

## V2.0 多租户与高并发扩展（2027-Q3，触发式）

| 项 | 内容 |
|---|---|
| 演进阶段 | V-004 |
| 触发条件 | 并发 Agent > 1000 OR MCP 数量 > 10000 OR R-03 用户 > 100K |
| 新增模块/接口 | M-008 WS 网关分片（按 agent_id hash）+ M-021 事件总线集群扩容至 9 节点 + DE-008/DE-009 数据库分片 |
| 废弃模块 | 无 |
| 迁移策略 | 灰度 + 双写（分片 PG 与原 PG 同步 1 个月） |
| 回滚策略 | 路由切回单库 + 双写期间数据自动合并 + 4h 内可回退 |
| 风险等级 | 高（数据库分片不可逆，需周密演练） |
| 来源 | [TD推断:依据 — PRD 性能指标可扩展性预留 NF-05] |

**演进路径预留覆盖率：4 阶段 / PRD backlog 估算 6 个未来方向 = 67% ≥ 60% ✅**

---

# 第二部分：性能容量预判表（PC）

> 8 个关键性能指标，全部含基准测试建议 [R26]

| 性能指标 | PRD 来源 | 预估规模（MVP→V2.0） | 架构应对策略 | 瓶颈模块 | 扩展方式 | 基准测试建议 |
|---|---|---|---|---|---|---|
| 市场首页 LCP P95 | NF-01 / S-01 | 1.5s 内（1000 并发） | CDN 静态预渲染 + Redis 缓存 + jitter 防雪崩 | M-001 | 增加 SSR Pod 副本 + 多 CDN 边缘节点 | 1000 并发请求/30s，LCP P95 ≤ 1.5s，缓存命中率 ≥ 95% |
| tool_call 投递 SLO | NF-05 / S-05 / F-014 | 99.5%（50 并发/Agent） | WS ack + 5s 重发 + 令牌桶限流 100 req/s + event_id 去重 | M-008 | WS Gateway 按 agent_id hash 分片 | 50 并发 tool_call × 100 Agent，SLO ≥ 99.5%，断线 30s 内重连成功率 100% |
| dry-run 沙箱响应 | F-021 | 30s 硬上限（10 并发任务） | Docker 池化 + 任务队列 + CPU/Mem cgroup 隔离 | M-013 | 沙箱 Worker 水平扩展 + 多 Runtime 池 | 10 并发 dry-run，P95 ≤ 25s，超时率 ≤ 5% |
| 审计日志落盘 | NF-06 / F-017 | P95 ≤ 50ms（1000 TPS） | Kafka/NATS 异步 + 批量落盘 + WAL | M-010 | 审计 Sink 水平扩展 + DB 读写分离 | 1000 TPS 写入 5min，P95 ≤ 50ms，0 数据丢失 |
| 告警延迟 | F-030 | ≤ 1min | Prometheus 1min scrape + AlertManager 5min 去重窗口 | M-017 | AlertManager 集群化 | 注入 5min 错误率 30% 模拟，告警延迟 ≤ 1min |
| 通知延迟 | F-033 | ≤ 5min | MQ 异步 + 站内信批量推送 + per-MCP 过滤 | M-020 | 通知调度器水平扩展 + MQ 分区 | 10K 用户批量通知，延迟 ≤ 5min，到达率 ≥ 99% |
| 收藏夹响应 | F-031 | P95 ≤ 200ms（200 上限） | Redis 缓存 `fav:{user_id}` TTL 1h | M-018 | Redis Sentinel 集群 | 并发 100，P95 ≤ 200ms，缓存命中率 ≥ 90% |
| 安装→ready | F-004 | 5s 内 | M-013 沙箱预热 + M-022 分布式锁防并发 | M-002 + M-013 | Runtime 进程池预热 | 50 并发安装，P95 ≤ 5s，幂等性 100% |

**性能应对策略覆盖率：8/8 = 100% ✅（R17 满足）**

---

# 第三部分：结构风险清单（SR）

> 15 条结构风险，全部映射到具体反例/调研缺口

| 风险编号 | 描述 | 影响模块 | 影响范围 | 缓解策略 | 监控指标 | 来源 |
|---|---|---|---|---|---|---|
| SR-001 | 并发安装同 MCP 产生双实例 | M-002 | 单 R-03 用户路由冲突 | M-022 分布式锁 `lock(user_id, mcp_id)` 5s | 锁竞争次数/h / 双实例事件数 | [CE-001] |
| SR-002 | CVE 紧急 patch 被 LRU 误清理 | M-005 / DE-003 | 紧急 patch 丢失，R-03 升级失败 | LRU 跳过 `is_pinned OR is_critical_patch` 字段 | 清理次数 / 误清理事件数 | [CE-002] |
| SR-003 | 工具名 hash 6 字符碰撞 | M-006 / M-012 | 同 Agent 工具名冲突，运行时报错 | 写入前唯一性校验，碰撞扩展至 8 字符 hash | 碰撞次数 / 唯一性校验失败次数 | [CE-003] |
| SR-004 | Runtime 注入后立即崩溃 | M-007 | 工具调用空白返回无 error_code | 30s 周期健康检查 + `mcp.runtime.unhealthy` 事件 | Runtime 健康检查失败率 / 空白调用率 | [CE-004] |
| SR-005 | WS 50 并发 ack 重发风暴 | M-008 | 拥塞 + SLO 失效 | 令牌桶限流 100 req/s + 超限告警 | 重发 QPS / 拥塞次数 | [CE-005] |
| SR-006 | 沙箱后端不可用静默降级 | M-013 | 危险 MCP 入库无隔离 → 运行时 RCE | R-01 强制阻断 + 升级环境通知 | 后端不可用次数 / 降级阻断次数 | [CE-006] |
| SR-007 | DNS 重绑定跨对象失效 → SSRF | M-013 / M-007 | 内网攻击成功 | DE-011 `dns_pinning_enabled` + 重定向后重新校验 | SSRF 告警次数 / 重绑定检测次数 | [CE-007] |
| SR-008 | 用户自创 PostgreSQL 模板含 CVE | M-011 / DE-018 | 下载者被攻击（CVE-2025-49596） | 模板发布强制静态分析 + CVE 规则库 | 拦截次数 / 触发 CVE 规则次数 | [CE-008] |
| SR-009 | 工具名 64 字符截断越界 | M-006 / M-012 | Anthropic API 拒绝 | 截断后强制 ≤ 64 字符 + 自动扩展 hash | 越界事件数 | [CE-009] |
| SR-010 | 用户跳过权限同意 UI | M-014 | 审计无法追溯同意范围 | 按钮在 UI 完整渲染前 disabled + consent_text ≥ 100 字符校验 | 短 consent_text 事件数 | [CE-010] |
| SR-011 | 取消与重试冲突 | M-009 | 取消后仍重试，违反 2s SLA | cancel_token 检查 + 重试前 token 验证 | 取消失败次数 / 冲突事件数 | [CE-011] |
| SR-012 | 告警 5min 窗口边界重复 | M-017 | 同错误类型边缘触发两次告警 | dedup_key 含 minute_bucket | 边界重复告警次数 | [CE-012] |
| SR-013 | ack 重发导致审计日志重复 | M-008 + M-010 | DE-009 计数虚高 | event_id 作审计去重键 + SELECT-then-INSERT | 重复 event_id 数 | [CE-013] |
| SR-014 | 卸载与绑定竞态 | M-002 + M-006 | 绑定成功但 instance 已卸载 | M-022 共享 instance_id 锁 | 竞态事件数 / 锁等待次数 | [CE-014] |
| SR-015 | dry-run 误报率 MVP 10% 长期成本 | M-013 | R-02 反复提交 + 平台审核压力 | V1.1 演进路径 LLM 增强至 2% | 误报率（周）/ R-02 重试次数 | [SA:R-014 + CR-002] |

**结构风险覆盖率：14 反例 + 1 调研缺口 = 15/15 = 100% ✅（R11 洞察注入满足）**

---

[阶梯退出检查] ①演进路线 4 阶段全部含回滚：是 ②性能指标 8/8 含基准测试：是 ③风险 15 条全部映射反例/缺口：是 ④backlog P2/P3 预留率 ≥ 60%：是（67%）
