# 三层闭环判定（修订版）

> ⚠️ **路径 ERRATA（2026-06-03 整理）**：本文用于标注"代码空间未落地"的模块路径（`api/v1/mcp`、`application/mcp/`、`infrastructure/agentruntime/mcp_injector`、`infrastructure/db/models/mcp.py`、`docker_sandbox` 等）为修订前命名。实际目标落点以 `06-详细设计/FS-MCP §1` + `docs/specs/04-commands §2.6` 为准（URL `/api/mcp/`，无 `/v1/`）。

> **版本**：V1.0-rev（2026-06-03 重写）
> **棒位**：⑧ 系统运行模拟与闭环
> **结论先给**：
> - 🟢 **PRD 迭代闭环** + **下游反馈闭环**：已建立并收敛（修订后无未消化反馈）
> - 🟡 **终局闭环**：**双口径判定**
>   - 计划空间（修订后 FS/SA/MD/IC/UI 文档自洽性）：🟢
>   - 代码空间（`src/backend/app/` + `src/frontend/src/` 真实落地）：🔴 须 P1-P4 实施后翻转为 🟢
> **关键修订**：原版"🟢 三层闭环均建立且收敛；系统可在零修改下走通端到端主路径"——**该结论仅在计划自身虚构的 `src/agenthub/` 仓内成立，对真实代码库不成立**。本修订版做明确的口径分离。
> **单一权威入口**：[`../README-REVISION.md`](../README-REVISION.md)

---

## 0. 修订要点

| 维度 | 上一版 | **修订版** |
|------|--------|-----------|
| 闭环判定 | 🟢 三层闭环均建立且收敛 | 🟢 + 🟡 双口径（计划空间 🟢 / 代码空间 🔴） |
| "零修改走通"语境 | `src/agenthub/` 虚构仓 | **双空间判定**：修订后映射到 `src/backend/app/` 后，🔴 不可走通 |
| 修订决策 | 0 决策 | 4 项决策已落（§README §7） |
| 下一棒指引 | "建议进入生产化准备" | "**先 P1-P4 4 阶段实施**，完成后再判定代码空间闭环" |

---

## 1. PRD 迭代闭环（① ⇄ ②）

### 1.1 是否建立
**是**。`01-需求澄清/PRD-MCP-V1.3-20260602.md` 已完成 V1.0 → V1.2 → V1.3 三轮迭代；V1.3.1 errata（2026-06-03 追加）落地 4 项修订决策。

### 1.2 修订版本

| 版本 | 起点 | 终点 | 主要修订 |
|------|------|------|----------|
| V1.0 | 用户原始需求 | 初始 PRD | 30 功能点 + 13 不做清单 |
| V1.2 | V1.0 | 第二版 | 新增 F-013 SDK Adapter、调整 B-04 |
| V1.3 | V1.2 | 终版 | 新增 F-022 模板库；Codex/Trae 移下期 |
| **V1.3.1**（errata） | V1.3 | **修订版权威** | 4 项决策：①表名改 `workspace_mcp_installations`（E-01）②SDK Adapter 移下期（E-02）③dry-run 简化版（E-03）④前端真实状态明确（E-04） |

### 1.3 V1.3.1 errata 4 项决策吸收

| 编号 | 议题 | 吸收位置 |
|------|------|----------|
| E-01 | 表名 | `06-详细设计/MD-MCP-V1.0-20260602.md` §1.2；`06-详细设计/IC-MCP` §1.2 |
| E-02 | SDK Adapter 移下期 | `PRD §3.2 F-013` 备注；`README-REVISION §3.3` CLI Adapter `attach_mcp(...)` 扩展点 |
| E-03 | dry-run 简化版 | `06-详细设计/FS-MCP §3.3`；`TA-MCP §0` |
| E-04 | 前端真实状态 | `06-详细设计/MCP-UI-frontend-V1.0-20260602.md` §0（新增文档） |

### 1.4 闭环判定
🟢 **PRD 迭代闭环完整建立、3 轮迭代、4 项 V1.3.1 errata 全部吸收**。

---

## 2. 下游反馈闭环（任一棒 → 上游）

### 2.1 是否触发
**是**。2026-06-03 修订过程中，**真实代码空间**扫描触发了对原版的全面回退与重写：

| 触发棒 | 触发原因 | 反馈形式 | 上游动作 | 是否收敛 |
|--------|----------|----------|----------|----------|
| ②RA（评审） | `src/frontend/src/` 与 `src/backend/app/` 内 MCP 文件 0 命中 | 12 项可行性清单 | ①PM 修订 PRD V1.3.1 + ③SA 改写 + ⑤AR 改写 + ⑥DD 改写 + ⑧判定改写 | ✅ 已收敛 |
| ③SA-001 | 4 层反向语义 | 修订版 | 改写为 5 层洋葱 | ✅ |
| ⑤AR-001 | 22 模块 / 30 实体 / 132 异常规模 | 修订版 | 砍到 MCP 主链路必需项 + 下期 NB-02 | ✅ |
| ⑥DD-001 | 引用虚构 `src/agenthub/` 仓 | 修订版 | 改写到真实 `src/backend/app/` | ✅ |
| ⑧判定-001 | "零修改走通"语境模糊 | 修订版 | 双口径：计划空间 🟢 / 代码空间 🔴 | ✅ |

### 2.2 是否回退到上游棒
**全面回退一次**（2026-06-03 一次性回退到 ①PM，重写 8 棒中的关键文档）。等价于 5 棒均触发，1 次大盘回退。

### 2.3 是否最终收敛
🟢 **下游反馈闭环 5 棒触发、1 次大盘回退、修订后 4 阶段无未消化反馈、全局收敛**。

---

## 3. 终局闭环（双口径）

### 3.1 计划空间（🟢）

> 判定对象：修订后 `README-REVISION.md` + `FS-MCP` + `SA-MCP` + `TA-MCP` + `MD-MCP` + `IC-MCP` + `MCP-UI-frontend` 共 7 份关键文档的**自洽性**。

| 维度 | 判定 | 依据 |
|------|------|------|
| 接口契约可调用 | ✅ | 8+2 端点 IC-MCP §1-2 已写；PR-01 待冻结（明确 P1 启动前完成） |
| 模块文件已就位 | ✅ | FS-MCP §1-3 已映射到 `src/backend/app/` 5 层 + `src/frontend/src/` 真实路径 |
| 数据实体已建表 | ⏳ | MD-MCP §1 4 张表已写；alembic 0006-0009 待 PR-09 后落地 |
| 业务规则有仲裁 | ✅ | `domain/mcp/rules.py` 8 条规则已集中 |
| 异常有兜底 | ✅ | IC-MCP §4 18 个 E_MCP_* 错误码 |
| 性能 SLO 满足 | ⏳ | S-01~07 沿用 PRD V1.3；待 P4 验证 |
| 安全约束满足 | ✅（本期最小化） | 沿用既有 JWT + 干跑单 Docker（E-03） |
| 可观测完整 | ✅ | trace_id 贯穿（不用 OTel，B-11） |

🟢 **计划空间：8 项中 6 项绿、2 项待 P4 验证**。

### 3.2 代码空间（🔴）

> 判定对象：真实 `src/backend/app/` 与 `src/frontend/src/` 的 MCP 落地。

| 维度 | 判定 | 依据 |
|------|------|------|
| 后端 4 表 SQLAlchemy 模型 | ❌ | `infrastructure/db/models/mcp.py` 不存在 |
| 后端 alembic 0006-0009 迁移 | ❌ | 既有 0001-0005 之外无 MCP migration |
| 后端 `domain/mcp/` 4 实体 | ❌ | 不存在 |
| 后端 `application/mcp/` 5 服务 | ❌ | 不存在 |
| 后端 `infrastructure/agentruntime/mcp_injector.py` | ❌ | 不存在（AR-02 关键扩展点） |
| 后端 `infrastructure/docker_sandbox/runner.py` | ❌ | 不存在（E-03 干跑） |
| 后端 `api/v1/mcp.py`（8 端点） | ❌ | 不存在 |
| 后端 `api/ws/toolcall.py` | ❌ | 不存在（F-014 复用） |
| 前端 `pages/McpMarket*` + `McpCreatePage`（3 页） | ❌ | `pages/` 目录不存在 |
| 前端 `components/mcp/*`（6 组件） | ❌ | `components/mcp/` 目录不存在 |
| 前端 `components/agent/McpBindingPanel.tsx` + Tab | ❌ | 不存在；既有 AgentDetailPage 4 Tab 不含 MCP |
| 前端 `stores/mcpStore.ts` | ❌ | 不存在 |
| 前端 `routes.tsx` | ❌ | 不存在（无路由注册） |
| 前端 `api/mcp.ts` | ❌ | 不存在 |
| 端到端真实可走通 | 🔴 **否** | 0/15 后端模块 + 0/11 前端模块 |

🔴 **代码空间：0/15 后端 + 0/11 前端 = 0% 落地**。

### 3.3 走通条件（必须满足才能翻 🟢）

详见 `end-to-end-trace.md` §4 4 阶段清单。**P1 → P3 → P2 → P4 全部完成后，代码空间由 🔴 翻转为 🟢**。

### 3.4 若不能，应回退到哪棒？
**不适用**（当前 4 阶段未启动，无中间态需要回退）。

### 3.5 兜底建议（非回退，仅优化）

| 序 | 建议 | 来源 | 影响 |
|----|------|------|------|
| 1 | S-03 dry-run 误报率 2% 在沙箱矩阵版（NB-02）后再校准 | PRD S-03 | 触发 PRD V1.4 |
| 2 | `mcp_tool_call_logs` 90 天后归档策略（下期引入 TimescaleDB 分区） | MD-MCP §1.4 | 性能 |
| 3 | 真实浏览器 headless Chrome 截图核验（MCP-UI §5） | UI 切片 | 体验 |
| 4 | SDK Adapter（NB-02.1）延 `attach_mcp(...)` 扩展点 | ADR | 范围 |
| 5 | Codex / Trae Runtime 接入下期排期 | PRD NB-01 | 范围 |

---

## 4. 三层闭环总判定（修订版）

| 闭环 | 上一版 | **修订版（计划空间 / 代码空间）** |
|------|--------|----------------------------------|
| PRD 迭代闭环（①⇄②）| 🟢 | 🟢 / —（仅设计空间） |
| 下游反馈闭环（③~⑥→上游）| 🟢 | 🟢 / —（仅设计空间） |
| 终局闭环（⑧判定）| 🟢（虚构空间） | 🟢（计划空间） / 🔴（代码空间） |

**总评**：
- 计划空间 🟢 三层闭环已建立并收敛
- 代码空间 🔴 须 P1-P4 4 阶段实施后翻转为 🟢
- 不存在"零修改可走通"——计划空间自洽 ≠ 代码可走通

---

## 5. 后续动作

1. **P1 启动前**：先 PR-01 冻结 8+2 端点到 `docs/specs/04-commands` §MCP（2 人 Review，PR-06）
2. **P1 启动时**：先 PR-09 同步 `docs/specs/03-data-model` §MCP（CR-03）
3. **P1-P4 实施**：按 `end-to-end-trace.md` §4 清单执行
4. **P4 收束**：ADR + 收束报告 + NB-02 清单
5. **本判定复审**：P4 完成后重写本 closure-verdict，将代码空间从 🔴 翻转为 🟢

---

*本 closure-verdict 是 MCP 接入**闭环判定**唯一权威。修订后明确分离"计划空间"与"代码空间"双口径，避免原版"虚构空间自洽"的误判。*
