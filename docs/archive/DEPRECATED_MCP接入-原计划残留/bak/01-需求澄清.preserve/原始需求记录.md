# 原始需求记录（用户原话保留 + 解析对照）

> **记录原则**（soul R6）：用户原话必须保留，标注解析结果

## 用户原话（V0，2026-06-02 输入）

```
为 AgentHub 项目（IM 聊天式多 Agent 协作平台，5 层洋葱架构：L1 Infrastructure →
L2 Domain → L3 Application → L4 API → L5 Presentation；技术栈 FastAPI + React/TS
+ PostgreSQL + Redis + Docker；CLI/SDK 双轨 Adapter，3 个已实现 Runtime：
ClaudeCode / OpenCode / PiAgent，Codex / Trae 规划中）添加 MCP（Model Context
Protocol，Anthropic 2024-11 开源、2025-06-18 最新规范）功能。

**核心范围（用户原话「我没有写完，帮我补充」）**：
1) **MCP 市场页面** —— 用户浏览/搜索/分类筛选/查看 MCP server 详情（manifest /
工具列表 / 依赖 / 权限）/ 一键安装到当前 workspace / 进程管理（stdio 拉起 /
SSE 拉 manifest / 闲置超时）
2) **为 Agent 接入 MCP 功能** —— Agent 配置页新增「MCP 接入」Tab，展示已绑定
MCP、绑定新 MCP、解除绑定、调整 config override；CLI Adapter 在 Agent 启动时
读 bindings 注入 --mcp-config；工具调用前/中/后通过 WebSocket 下行
tool_call/tool_result 事件
3) **创建 MCP 功能** —— 用户提交 stdio（command+args+env）/ sse（URL+auth）/
http 类型 MCP server；模板库（filesystem / github / fetch / brave-search /
postgres 等 5 个内置）；提交前 dry-run 沙箱验证（subprocess + timeout + 资源限制）

**关键约束**（用户原话）：
- 严格遵循开发规范/（CLAUDE.md + conventions/01~08 + ai-workflow 7 篇）
- 不破坏 5 层洋葱依赖方向 L5 → L4 → L3 → L2 ← L1
- CLI 优先（ADR-0001）
- 单进程池 / 闲置超时（5 min）/ 进程监控（healthcheck 30s）/ secret 日志脱敏
- 流程档位：标准
```

## 解析对照

| 用户原话片段 | 解析结果（PM 视角） | PRD 映射 |
|---|---|---|
| 「MCP 市场页面 — 浏览/搜索/分类筛选/查看详情/一键安装/进程管理」 | 拆为 6 个功能点 F-001~F-006 | §3.1 |
| 「std io 拉起 / SSE 拉 manifest / 闲置超时」 | stdio 拉起 = F-014 dry-run；SSE 拉 manifest → 修订为 Streamable HTTP (S-001)；闲置超时 = F-007 | F-007, F-014, F-016 |
| 「Agent 配置页新增「MCP 接入」Tab」 | F-009 | §3.2 |
| 「CLI Adapter 在 Agent 启动时读 bindings 注入 --mcp-config」 | F-012 | §3.2 |
| 「WebSocket 下行 tool_call/tool_result 事件」 | F-013 | §3.2 |
| 「提交 stdio（command+args+env）/ sse（URL+auth）/ http 类型」 | F-015 (stdio) + F-016 (Streamable HTTP 取代 sse/http) | §3.3 |
| 「5 个内置模板」 | F-017 (filesystem / fetch / github/github-mcp-server / brave-search / postgres) | §3.3 |
| 「提交前 dry-run 沙箱验证」 | F-014 | §3.3 |
| 「单进程池 / 闲置超时（5 min）/ healthcheck 30s / secret 日志脱敏」 | F-007 / F-008 / NF-04 | §3.1 + §4 |
| 「我没有写完，帮我补充」 | 用户明确授权 PM 补充范围，PM 已补全 12 个二级功能点 | §3 + §4 |
| 「CLI 优先（ADR-0001）」 | CLI Adapter 优先于 SDK，PRD 中 F-012 已体现 | §3.2 |
| 「5 层洋葱 L5 → L4 → L3 → L2 ← L1」 | NF-08 约束，AR-01~06 lint 100% 通过 | §4 |
| 「Codex / Trae 规划中无 runtime 文件，PRD 不可假托有对应文件」 | NB-01 强化 | §5 |

## V0→V1 增量（RA-001 调研反馈）

| 编号 | RA 反馈 | 采纳 | 修订条目 |
|---|---|---|---|
| S-001 | SSE → Streamable HTTP（2025-06-18 规范） | ✅ | F-016 |
| S-002 | 沙箱补全跨平台 | ✅ | F-014 |
| S-005 | 工具命名 + 64 字符硬限制 | ✅ | F-011 |
| S-006 | 5 模板 GitHub 迁移 | ✅ | F-017 |
| S-008/S-011 📌 | 进程池自建 + mcp-proxy 架构参考 | ✅ | NF-10 |
| S-009 📌 | 30 天 allowlist | ✅ | F-024 |
| S-016 | Codex/Trae 状态注 | ✅ | NB-01 |
| S-021 | 进程池 32→64 | ✅ | F-008 |
| S-010 | K4 误判率 5%→15% | ✅ | F-018 |
| S-018 | 审批超时 5min→10min 软+30min 拒 | ✅ | F-023 |
| S-022 | 网络白名单 Docker 优先 | ✅ | F-021 |
| S-012/13/14/15/17/19/20/23/24 | 次要修订 | ✅ | 散落条目 |

**采纳率**：11/11 = 100%（无 ❌ 不可行项）
