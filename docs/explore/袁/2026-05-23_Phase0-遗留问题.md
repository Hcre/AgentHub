# Phase 0 执行后遗留问题

> 日期: 2026-05-23 | 优先级: P0=阻塞, P1=建议
> 最后更新: Claude 修复后

---

## ✅ P0 — CSS 入口指向错误（已修复）

`main.tsx` 已改为 `import './styles/index.css'`

---

## ✅ P0 — 残留 Vite 默认样式文件（已清理）

`App.css`、`index.css` 已删除，`App.tsx` import 已移除

---

## ✅ P1 — App.tsx 重写（已完成）

占位页用 Zustand + lucide-react + glass-panel，含亮/暗切换

---

## ✅ P1 — Zustand store 已创建

`src/stores/uiStore.ts` — theme + toggleTheme

---

## ✅ P1 — index.html title 已更新

`<title>AgentHub</title>`，且 `lang="zh-CN"`，移除未用的 demo `icons.svg`

---

## ✅ P1 — src 骨架子目录已补齐

`types/` `hooks/` `api/` `components/{ui,layout,chat}/` 均建，含 `.gitkeep` 占位（组件 Phase 1 填入）

---

## ✅ P2 — Prettier 已配置

`.prettierrc.json`（semi:false / singleQuote / printWidth:100）+ `format` / `format:check` 脚本

---

## ✅ 部署改造（转换方案 §4，已完成）

旧 Dockerfile/nginx.conf 已随原型移入 `prototype/`；根目录新建**多阶段构建** Dockerfile（node:22 build → nginx 托管 dist）+ nginx.conf（`index.html` + SPA 回退）+ `.dockerignore`。docker compose 重建验证：http://localhost:5173 返回新构建（title `AgentHub`、hash 资源、SPA fallback 200）。

---

## 验收 Gate（全绿）

`tsc -b` 0 错误 · `vite build` 产出 dist · `eslint .` 0 · `prettier --check` 全通过 · dev server 起（5173 被占时自动 5174）· 生产容器 5173 OK
