# === Python ===
__pycache__/
*.py[cod]
*.db
*.sqlite3
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# === Node ===
node_modules/
dist/
dist-ssr/
*.local
.vite/

# === Env / secrets ===
.env
.env.*
!.env.example
*.pem
*.key

# === IDE ===
.vscode/
.idea/
*.swp
.DS_Store

# === Project runtime ===
.agent_workspace/
*.log
logs/
*.stackdump

# === Docker ===
docker/data/
.ace-tool/

# === Git worktrees ===
.worktrees/

# === Runtime / personal artifacts ===
# 统一容纳：scraps/（草图）+ screenshots/（截图）+ uploads/（运行时上传）
_assets/

# === AI session caches ===
.deepseek/
.reasonix/
.claude/

# === Agent 工作区（mavis / opencode 等 agent 自己的本地状态，不入项目）===
.mavis/
.opencode/
matrix-media-*.png
matrix-media-*.mp3

# === 代码图谱（conventions/08）===
# 生成产物可由 scripts/gen_codegraph.py 重建，不入库；
# 仅 graph_schema.json（节点/边定义）入 git。docs/CODE_MAP.md 照常入库。
.codegraph/graph.json
.understand-anything/
# 锚定到仓库根：原裸 `backend/` 会误伤源码树 src/backend/，
# 导致所有新增后端文件被静默忽略（2026-06-03 修正）。仓库根无 backend/ 目录。
/backend/

# === 本地已装 skill（per-user，不入库；重装从市场再拉）===
.agenthub/skills/
# === 调试 / 测试 artifacts（不进库）===
.agenthub-skills-test.json
.lib-test.json
.lib-test2.json
.tmp/
src/.tmp/
src/frontend/.tmp/
src/frontend/diag.mjs
src/frontend/tmp-shoot.mjs
.worktrees/

# === 附件存储（P0-3 上传落盘）===
src/backend/storage/

# === 调试 / 临时产物（不入库；git 历史已 commit 的需要 git rm --cached 移除）===
docs/deliverables/video/_work/
docs/deliverables/video/_dbg/
docs/deliverables/video/_work-debug/
scripts/probe_*.py
scripts/verify_frames.py
scripts/e2e-*.py
scripts/hover-*.py
scripts/dom-*.py
scripts/msg-*.py
scripts/retest-*.py
docs/deliverables/video/_work/*.js
docs/deliverables/video/_work/*.py
.agenthub/templates/
.agenthub/templates/
.agenthub/

# === Local-only media (>100MB, ship via GitHub Release Assets) ===
# 视频/ 目录下的录屏不入库,见 docs/deliverables/INDEX.md §🎬 演示视频
视频/
