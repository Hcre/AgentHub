# 工作日志：API 联调收尾 — WS 主路径确认

- **谁**: 袁
- **日期**: 2026-05-24
- **分支**: feature/chat/ws-send-wiring
- **关联 Spec**: spec/roadmap_开发路线图.md（M2 §2.1/2.5）、docs/task-assignment_任务分配.md（袁 §7.1/7.2）

## 目标

收尾前端 §7.1/7.2 API 联调：将 `chatStore.send()` 的 setTimeout 假回复替换为真实 WS 发送。

## 产出

- [x] **结论：§7.1/7.2 已收尾**，无需代码改动。
- [x] 核对发现：合并进来的 main（`8c01ac8 feat(frontend): 前后端 API 联调集成`）已经把 WS 主路径接好。
- [x] 更新 STATUS.md：袁那一行标记 §7.1/7.2 完成；补齐 Git↔目录映射（`xiangbianpangde → 袁`）。

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 不替换/删除 `chatStore.send()` 的 setTimeout | 它已不是主路径，而是 `sendMessage()` 返回 false（WS 未连接 / mock agent / 后端不可用）时的**降级兜底**，见 `ChatView.tsx:60-64` 与 `chatStore.ts:60` 注释 | 保留离线/mock 场景的反馈，不破坏降级体验 |
| §7.1/7.2 视为已完成 | WS 发送（`useWebSocket.sendMessage`）已在 `ChatView.onSend` 作为优先路径调用 | roadmap M2 单聊 MVP 闭环（发消息→流式回复）前端侧已通 |

## 当前真实数据流（已就位，非本次新增）

```
Composer.onSend
  → ChatView.onSend(text)
    → sendMessage(text)            // useWebSocket：WS OPEN 时 addUserMessage + ws.send，返回 true
       └─ 失败(false) → send()     // chatStore：setTimeout 假回复，仅 mock/离线兜底
  → ws.onmessage → applyStreamEvent // 流式 text/done/error 落到对应 convKey 消息桶
```

Session 建立见 `ChatView.tsx:33-47`：首次进会话调 `sessionsApi.createPrivate`，失败则维持 mock 降级。

## 未完成 / 阻塞

- 无。

## 给下一位的交接

> §7.1/7.2 已收尾，前端单聊 MVP 闭环（建 Session → WS 发消息 → 流式回复）已通，离线/mock agent 自动降级到本地假回复。
> 下一步按 docs/task-assignment_任务分配.md 袁 §1.5 起：**@mentions 解析 + 前端补全菜单 + dispatch_mode=auto 路由**（依赖董的群组）。
> 注意：`send()` 的 setTimeout 是**有意保留的降级兜底**，不要当死代码删除——删了会让 mock agent / 后端挂时的发消息没有任何反馈。
