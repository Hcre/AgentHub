# 工作日志：Phase 5 — 次要视图（活动/日历/收件箱/助手详情/技能·记忆·设置）

- **谁**: 袁
- **日期**: 2026-05-23
- **分支**: xbpd
- **关联 Spec**: `docs/前端实施计划_v1.md` §5

## 目标
补齐中心区其余 Tab 与左导航 section 的视图，全部 mock 驱动，可演示。

## 产出
- [x] 类型：Activity / InboxItem(+DiffLine) / Skill / MemoryItem / CalendarEvent / AgentProfile(+MemoryLevel/AgentConfig)
- [x] `src/data/extra.ts`：activity / skills / memory / inbox / agentProfiles / calendarEvents + TODAY_STR
- [x] `src/lib/date.ts`：parseDate/fmtDate/addDays/addMonths/startOfWeek + 月名/周名
- [x] §5.1 `ActivityFeed`：时间线活动卡（工具数 + 正文 + 运行中 + 搜索）
- [x] §5.2 `CalendarView`：月/周/日切换 + 今天/翻页；周/日网格按 start/end 定位事件、今日高亮、tone 着色；月视图 6×7 网格事件 chip
- [x] §5.3 `InboxView` + `inboxStore`：全部/审批/任务/系统 分类 Tab；审批卡含 diff（add/del/neutral 着色）+ 影响 + 批准/驳回（resolve 移除）；hover 标已读
- [x] §5.4 `AgentDetailPage`：概览（bio/负载/群组）/能力（tags）/记忆（L1-L4）/设置（config，API Key 掩码）+ 返回
- [x] §5.5 `SkillsView` / `MemoryView` / `SettingsView`（中心区 Tab）
- [x] CenterPanel 重构：section（group/agent-detail/tasks/inbox/calendar）+ chat 内 TabContent（chat/tasks/activity/calendar/skills/memory/settings）统一分发；成员/详情入口用 uiStore.viewAgent
- [x] 新增图标 chevronLeft/chevronRight
- [x] 验收全绿：tsc 0 / eslint 0 / prettier / build；dev 截图确认日历周视图（事件定位/今日高亮/月周日切换）

## 关键决策
| 决策 | 原因 | 影响 |
|------|------|------|
| 日历自写轻量 date.ts | 不引 dayjs/date-fns，mock 够用 | 周/日按 (hour-1)*56px 绝对定位事件 |
| inbox 用独立 store | 审批 resolve/markRead 需可变状态 | 后续接 /api/inbox + 群聊审批流（见 group/HANDOFF） |
| CenterPanel 用 TabContent switch | 9 个 Tab 集中分发，避免长三元 | 新增 Tab 只加 case |
| 助手详情/设置 API Key 掩码显示 | 红线：密钥不明文 | 仅占位掩码，编辑表单待 Phase 6 |

## 未完成 / 阻塞
- [ ] 频道(channels)、文件(files) 两个中心 Tab 仍占位。
- [ ] 收件箱「批准/驳回」仅从列表移除，未回写群聊消息状态 / 未触发后端执行（接 group/HANDOFF 审批流）。
- [ ] 日历事件只读，无新增/拖拽改时间；月视图点格不跳周。
- [ ] 活动卡工具列表未做展开；助手详情设置不可编辑。

## 给下一位的交接
> Phase 5 视图均 mock 只读。进 Phase 6（Agent 创建/编辑）：CreateAgentModal（表单 name/role/provider/model/api_key/skills/system_prompt，复用 Dialog/Input/Textarea + agentProfiles.providers 结构）；AgentDetailPage 设置 Tab 改可编辑 + 删除二次确认。
> 或进 Phase 7（mock→真实 API）：把各 store 初始化从 data/ 换成 API 调用，chat/group 接 WS。收件箱审批流是串起群聊 requiresApproval 的关键节点。
> 数据全在 `src/data/{mock,groups,extra}.ts`，类型在 `src/types/index.ts`，按 PRD §6 对齐后端。
