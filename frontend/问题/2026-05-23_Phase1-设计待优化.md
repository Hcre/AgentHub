# Phase 1 设计待优化项

> 日期: 2026-05-23 | 后续优化方向，非阻塞

---

## 1. LeftPanel AI 标识样式

**位置**: `src/components/layout/LeftPanel.tsx` 第 179 行

**当前**: AI 标识和展开/折叠按钮合并为一个元素（`bg-brand/10 px-1.5` + chevron）。

**原型**: AI 标识是独立的 `Badge variant="brand"`。

**建议后期改成**:
```tsx
<Badge variant="brand">AI</Badge>
<Icon name="chevronDown" />
```

**优先级**: 低，后续统一打磨时调整。

---

## 2. 消息气泡两侧头像展示

**位置**: `src/components/layout/CenterPanel.tsx`

**当前**: 用户消息和 Agent 消息两侧都显示 28px Avatar。

**原型**: 通常 Agent 消息带头像 + 名称，用户消息不重复显示头像。

**建议**: Phase 2 细化聊天 UI 时统一展示规则。

**优先级**: 低，当前可用。
