# 工作日志：E2E 视觉验证 + 群聊 P0-4/P0-5 修复 + G2 UX 改进 + 清理 + dev workflow 优化

- **谁**: Mavis (owner)
- **日期**: 2026-06-07 (11:00-13:45)
- **分支**: main（直推，无 PR 流程，per user preference 2026-06-07 落档）
- **关联 Spec**:
  - `docs/plan/背景.md` 6 大核心功能（IM 聊天/Orchestrator/多 Agent 接入/产物预览/部署/多端）
  - `docs/plan/开发清单_roadmap.md` §8 P0/P1
  - `CLAUDE.md` §红线 AR-02/CR-01~12/T-01~06/D-01~12/PR-01~09

## 目标
用户疑虑"功能都没实现" → 用 playwright + cu (Computer Use) 走真机 E2E 验证 → 发现 3 gap → 修代码 → 清理 unstaged 改动 → 优化 dev workflow（vite dev container + HMR 实时刷新）。

## 产出

### 代码修复 (3 commit)
- [x] `079cdca` — **G1 P0-4/P0-5 群聊消息接 Pin + 复制代码**（与私聊 MessageBubble schema 对齐；`GroupMessageItem.tsx` + 类型 + 11 单测）
- [x] `f41934b` — **G2 群组卡片整卡点击进群聊**（不再依赖 hover icon 入口；`GroupsListPage.tsx` article + onClick）
- [x] `d66ad3c` — Merge `feature/frontend/pin-ui` → main（包含 G1+G2）

### 清理 + 落档 (5 commit)
- [x] `811705d` — .gitignore 增 .mavis/ .opencode/ matrix-media-* ignore 规则
- [x] `43a7ff9` — `.harness/reins/` 12 个 Mavis reins agent 配置落档（mcp-architect / detailed-designer / developer / fixer / merge-checker / pm / researcher / skeleton-builder / system-analyst / tester / top-designer）
- [x] `86b8317` — 5:50 凌晨冲刺收束产物（deliverables + scripts + ADR + plan rename + verifier；128 files / 4920 insertions）
- [x] `0992270` — 完整 rename `背景_PRD_AgentHub_统一方案.md` → `背景.md`
- [x] `25e4dd9` — .gitignore 排除 `_work/_dbg` debug 产物 + `git rm --cached` 60+ debug 文件（72 files / 1006 deletions）

### Dev workflow 优化 (1 commit)
- [x] `f0a2cb5` — **vite dev container + volume mount**（改 Dockerfile + docker-compose + vite.config proxy 修复）
  - `src/frontend/Dockerfile` 多阶段 build → 单 stage dev（`CMD vite dev --host 0.0.0.0`）
  - `src/docker/docker-compose.yml` frontend 加 6 mount + `CHOKIDAR_USEPOLLING=true` + port 5174:5173
  - `src/frontend/vite.config.ts` proxy target `localhost:8000` → `backend:8000`（容器内用 docker-compose service name）

### 测试 + 验证
- [x] 11 个新单元测试（pin 7 + copy 4，全绿）
  - `src/frontend/src/components/group/__tests__/GroupMessageItem.pin.test.tsx`
  - `src/frontend/src/components/group/__tests__/GroupMessageItem.copy.test.tsx`
- [x] `npm run lint` — 0 错（CODE_FENCE_RE 改 `matchAll` 模式 + 删未用 import）
- [x] `npx vitest run` — 47/47 绿（pin 7 + copy 4 + 既有 36）
- [x] 10 个 E2E screenshot 存 `docs/deliverables/screenshots/e2e-F1..F10-*.png`（F1 S1 私聊 / F2 S2 群聊 / F3 AI 队友 / F4 任务看板 / F5 主题 / F6 创建群组 / F7 设置 / F8 私聊空 / F9 Pin hover / F10 Skill）
- [x] vite HMR 实时刷新验证（改 .tsx → `[vite] (client) hmr update /src/components/group/GroupMessageItem.tsx`）
- [x] Docker frontend 容器 rebuild + up -d 验证（Vite 8.0.14 ready in 345 ms, 0 警告）

### STATUS.md 更新 (3 段)
- [x] 新增 `🚧 2026-06-07 12:00 Mavis E2E 视觉验证` 段（11 章节结果表 + 3 新 gap + 工具沉淀索引）
- [x] 更新 `已知 gap` 段：S1 建议按钮不响应（实际是误判）+ S2 群聊卡片 UX 误导 + P0-4/P0-5 S2 群聊未实现
- [x] 新增 `🎯 2026-06-07 13:40 Mavis PRD 核心功能 vs 现状对照` 段（PRD 6 大功能 + 子功能状态表 = ✅ 完整 9 / ⚠️ 部分 6 / ❌ 未做 7 / 📋 计划 1）

### Memory
- [x] agent memory（`C:\Users\yhn\.mavis\agents\mavis\memory\MEMORY.md`）5 条新增：
  - cu MCP 测试协议（视觉+点击 = 首选 Computer Use）
  - cu MCP PowerShell JSON 注入坑（必须 --stdin + duration_ms 字段）
  - Windows ffmpeg gdigrab 录屏协议（-i desktop + -video_size 1920x1080）
  - Playwright 录 demo 视频核心约束（v4 wallpaper / v5 SetWindowPos crash / v6 --start-maximized）
  - Docker 容器 serve 静态资源时本地代码修改不生效（需 rebuild image 或 volume mount）
- [x] user memory（`C:\Users\yhn\.mavis\memory\user.md`）2 条新增：
  - 用户对 demo/录屏持怀疑态度，要求真机 E2E 验证
  - 用户偏好：直接 push main，不走 PR 流程

## 关键决策
| 决策 | 原因 | 影响 |
|------|------|------|
| 切到 playwright MCP 不用 cu 视觉测试 | cu 在 agentHub 上坐标精度 + Chinese encoding 双重失效（PS 5.1 单引号 JSON 被破坏）| 走 DOM 精准定位（getByRole + ref）+ Read image attachment 看图 |
| S1 私聊建议按钮"不响应"是误判 | input box 的 `beforeVal=""` → `afterVal="帮我看看当前项目的代码结构"` + `focused=true` —— setText 链完整 | 修了一条误报的 gap |
| 直接 push main 不走 PR 流程 | user preference 明确说"以后直接推送 main" | user memory 落档 + 跳过 PR-01/06/07 |
| vite dev container + volume mount | 改 frontend 代码不用每次 `docker compose build` (~3min) | Dockerfile 单 stage dev + 6 mount + CHOKIDAR_USEPOLLING |
| vite.config proxy target 改 backend:8000 | 之前 `localhost:8000` 在 frontend 容器内指向自己（无 8000 端口），proxy 失败 | nginx.conf 一直用 backend:8000；现在 vite 跟进 |
| .gitignore 排除 _work/_dbg + git rm --cached | 5:50 凌晨 commit (86b8317) 误包含 60+ debug 临时产物 | 72 files / 1006 deletions，但已 push 的 commit 86b8317 history 仍含 |
| Dockerfile 占位文件 + 后续 mount 覆盖 | build 期间 vite dev 启动需 index.html / vite.config.ts / tsconfig 存在 | mount 注入 host 实时文件；build 期间用占位 |

## 未完成 / 阻塞
- [x] ~~G1 S1 私聊建议按钮不响应~~ — 实际是误判（建议填输入框 + focused，setText 真工作）
- [x] ~~G2 S2 群聊卡片点击 💬 无响应~~ — 实际有"进入群聊"按钮（在卡片右下角），整卡点击是 UX 改进
- [ ] PRD 对照 6 个 ⚠️ 部分（对话列表搜索/置顶、消息类型部署卡、消息操作回复/引用、文档渲染、全屏预览、代码编辑器）— M5/MVP 范围，可补
- [ ] PRD 对照 7 个 ❌ 未做（含 5 个 P2 + Orchestrator 失败降级 + 代码冲突处理）— 计划外
- [ ] v6 录制脚本（替代 v4 wallpaper 残留）— 推迟到下个 session
- [ ] commit `86b8317` history 中 60+ debug 文件清理 — user 后续可 reset + 重新打包

## 已知 6 gap（STATUS.md line 30-34 + 104-130 累计）
1. E 视觉 S5 inbox 3 重 gap（backend TODO / frontend mock / UI 无 nav）— M4 TODO 标
2. S3 私聊 UI 不可达（ChatView mock-driven, LeftPanel 只 user-created）— 已 downscope
3. Pin API 无 session 所有权校验（probe 2 FAIL）— 需 backend 修复
4. Docker backend image 6h old 缺 `/api/attachments/*` — F 测试用 local uvicorn :8766
5. 视频 v4 录屏 DISPLAY1 wallpaper 残留（v5 Win32 SetWindowPos + ffmpeg crash 失败）— 已透明声明
6. P0-4/P0-5 S2 群聊未实现（之前）→ ✅ 修复（commit 079cdca）+ 群组卡片 UX 误导（G2 修复 commit f41934b）

## 给下一位的交接
> - Docker frontend 容器现在是 **vite dev mode**，改代码 HMR 自动刷新（<1 秒），**不用 rebuild image**
> - 5:50 凌晨冲刺 worklog 缺失，本 worklog 补完（`worklogs/mavis/2026-06-07_凌晨冲刺_收束.md`）
> - S2 群聊 demo 数据**没**含 ``` 围栏代码、URL 链接、diff 围栏、plan 类型消息 → 复制代码 / WebPreviewCard / DiffView / CoordinatorPlan 实际渲染需 trigger 真实数据才能截图
> - Pin/复制代码有 11 个单测，但 E2E 截图依赖 S2 群聊 demo 数据
> - 5174 容器跑 vite dev (5173) + 主机端口 5174，proxy backend:8000 走 docker-compose service name
> - 如果下次要切回 nginx serve 静态产物：参考旧 Dockerfile（多阶段 build + nginx.conf），重写回 build mode
> - 10 个 E2E screenshot 存 `docs/deliverables/screenshots/e2e-F1..F10-*.png` 可直接用
