# 工作日志：MCP PRD v1.1 修复（数字与名称矛盾）

- **谁**: 袁（Mavis `mcp-pm` agent 代笔）
- **日期**: 2026-06-02
- **关联**:
  - 主 PRD: [`docs/plan/MCP功能PRD.md`](../../plan/MCP功能PRD.md)（v1.0 → **v1.1**，414 → 433 行）
  - 主 worklog: [`worklogs/袁/2026-06-02_mcp-prd.md`](../../袁/2026-06-02_mcp-prd.md)（v1.0 首次提交记录）
  - 触发: verifier 反馈 v1.0 §1.2 '4 CLI Adapter' 数字与括号 5 名称矛盾
- **分支**: (未开，等评审组过审后开 `feature/mcp/prd-v1` worktree + 推 PR)
- **任务类型**: cosmetic 修正（同 session 重做即可）

## 目标

按 verifier 反馈，把 §1.2 中"4 CLI Adapter" 数字与括号内 5 个名称的矛盾修正为"3 CLI Runtime + Codex/Trae 规划中"，并消除文档内其他相同性质的数字不一致（§八 R3 + §十.3 P2 任务 3.4）。

## 修复明细

| # | 位置 | v1.0 | v1.1 | 修改方式 |
|---|------|------|------|---------|
| 1 | §1.2 L24 | 4 CLI Adapter（Claude Code / Codex / Trae / OpenCode / Pi Agent）| 3 CLI Runtime（Claude Code / OpenCode / Pi Agent，**Codex / Trae 规划中 [planned / integration]**，尚无对应 runtime 文件）| Edit 工具精确改 1 行 |
| 2 | §八 R3 L488 | 4 个 Runtime + Pi Agent / Codex CLI 降级 | 3 个已实现 Runtime + Codex / Trae 规划中 + Pi Agent 单独降级 | Edit 工具精确改 1 行 |
| 3 | §十.3 P2 任务 3.4 L560 | 4 个 CLI Runtime 注入点（ClaudeCode / OpenCode / PiAgent / Codex）| 3 个 CLI Runtime 注入点（ClaudeCode / OpenCode / PiAgent；Codex / Trae 规划中）| Edit 工具精确改 1 行 |
| 4 | 末尾新增 附录 C: Changelog | — | v1.0 → v1.1 完整 diff 记录 + 实测文件清单 | Edit 工具追加 19 行 |

## 验证

实测 `src/backend/app/infrastructure/llm/` 目录（2026-06-02 11:38）：
- ✅ `claude_code_runtime.py` (ClaudeCodeRuntime)
- ✅ `opencode_runtime.py` (OpenCodeRuntime)
- ✅ `pi_agent_runtime.py` (PiAgentRuntime)
- ✅ `claude_code_process_pool.py`（process pool 辅助，非独立 runtime）
- ✅ `factory.py`（Runtime 工厂，3 个 Runtime 调度逻辑）
- ❌ 无 `codex_runtime.py` / `trae_runtime.py` / `codex_adapter.py` / `trae_adapter.py`

**结论**：3 个 CLI Runtime 是事实，4 / 5 都是错的。Codex / Trae 是计划中（roadmap 提及但未实现）。

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 改"3 CLI Runtime"而非"5 CLI Adapter" | 现状文件清单是 3（事实优先）| 与 §十.3 任务数对齐 |
| Codex / Trae 标 "planned / integration" | 出现在原括号内但无 runtime 文件，roadmap 也未实现 | 不删除（保留规划），不计入 runtime 数字 |
| 同步改 §八 R3 + §十.3 任务 3.4 | 同样的 4 → 3 矛盾，否则文档内部不一致 | 评审一眼可验 3 处对齐 |
| 用 Edit 工具精确改 1 行 | 避免大改破坏 11 块结构 | git diff 干净（3 行修改 + 19 行新增）|
| 不动 STATUS.md / roadmap / 主 worklog | 任务说"不动" | 评审组只看 PRD 修改，不被噪音干扰 |
| 末尾加 Changelog 附录 C | 记录从 v1.0 → v1.1 的精确 diff | 评审可一眼验改动范围 |

## 影响评估

- **结构**：11 块 + 2 附录保持不变 → 新增 附录 C（Changelog）= 11 块 + 3 附录
- **行数**：414 → 433（+19）
- **架构 / 数据模型 / API / 前端**：零变更
- **可读性**：✅ 改善（数字与名称一致）
- **评审组过审成本**：✅ 降低（消除前后矛盾）

## 给下一位的交接

- **Mavis（评审组）**：
  - v1.1 是 v1.0 的最小补丁，3 处文字 + 1 处 changelog 附录
  - 重点看：§1.2 L24（主修复点）+ 附录 C（diff 记录）
  - 11 块结构不变，10/11 块完全没动
  - 评审过审后可一次 push v1.1 → Plan B-P1 实施

- **董 / 黎**：
  - 无影响
  - 若有反馈 Codex / Trae 何时实现 → 看 §九 不做项与 roadmap §十 阶段表（暂未列入 P1-P4）

- **袁**（自己）：
  - 修复完即可，等评审组过审
  - 若 verifier 还有 v1.2 反馈（可能性低，已修完 2 个 cosmetic 数字错），按相同模式增量修

## 临时约定

- 本次修复**未 commit**（按 03-git 红线走 worktree + PR 流程）
- 评审过审后第一次 commit: `docs(plan): MCP PRD v1.1 (数字修正 4→3 + Codex/Trae 标 planned)`
- 主 worklog `2026-06-02_mcp-prd.md` 不动（v1.0 提交记录），本 worklog 是 v1.1 增量

## 未完成 / 阻塞

- [ ] 评审组过审 v1.1（待启动）
- [ ] 评审过审后开 worktree `feature/mcp/prd-v1` + 推 PR
- [ ] PR-06 ≥1 Approve → merge main
- [ ] merge 后启动 Plan B-P1 实施

## 备注

本次 v1.1 修复验证了 PM agent 的核心职责：
1. **接收反馈** — 认真读 verifier 反馈，不擅自升级为"全面重写"
2. **最小修复** — 只改需要改的 3 处，不动无关内容
3. **可追溯** — 在 Changelog 附录 C 记录 v1.0 → v1.1 完整 diff
4. **结构保护** — 不破坏 11 块结构，append Changelog 而非改 §十一 验收标准
5. **事实优先** — 实测 3 个 runtime 文件清单，不靠记忆或 ADR 数字
