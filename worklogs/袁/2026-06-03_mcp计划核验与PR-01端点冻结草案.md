# 2026-06-03 · MCP 接入计划核验 + 路径校正 + PR-01 端点冻结草案

> 作者：袁（Claude Agent 协助） · 类型：计划核验 + 文档治理

## 背景
对 `docs/plan/后续升级计划/MCP接入/README-REVISION.md`（修订版单一权威）做落地前可行性核验，并按用户决策「两步都做」：① 修路径偏差 ② 起草 PR-01 端点冻结。

## 核验结论
修订版方向正确、对真实代码树的纠正属实（逐项已验证）：
- `src/backend/app/` 5 层洋葱存在 ✓；CLI Adapter 在 `infrastructure/llm/` ✓；alembic 0001-0005 ✓，续 0006 ✓
- roadmap §十 已指向 README-REVISION ✓；PR-09 已部分同步（03-data-model §MCP 4 表、01-architecture §MCP）✓
- 12 项可行性问题诊断与处置准确

但发现 **§3 落点路径仍有同类「写错路径」偏差**，且已被同步进 spec 01 §MCP.1，需冻结前修正。

## 改动（纯文档，未碰代码）

### Step 1 — 路径偏差校正
对照真实约定，修 `README-REVISION.md` §3.1/§3.3 + `docs/specs/01-architecture` §MCP.1/§MCP.2：
| 错误写法 | 校正为 |
|---|---|
| `infrastructure/agentruntime/mcp_injector.py` | `attach_mcp` 抽象方法落 `domain/llm/protocol.py::AgentRuntime(ABC)`，实现落 `infrastructure/llm/*_runtime.py` |
| `infrastructure/db/models/mcp.py`（包） | 单文件 `infrastructure/db/models.py` 追加 4 表 |
| `api/v1/mcp.py` | `api/routers/mcp.py`（无 `v1/` 目录） |
| `application/mcp/*.py`（子包） | `application/services/mcp_*_service.py`（扁平） |
| `infrastructure/docker_sandbox/runner.py` | `infrastructure/mcp/dry_run.py` |
| `BaseAgentRuntime`/`SpawnHandle`（虚构） | 真实契约 `AgentRuntime.stream()/stop()`，新增 `attach_mcp(bindings)->None` |
- 保留正确项：`domain/mcp/` 子包（与 `domain/llm/`、`domain/task_engine/` 先例一致）
- 口径对齐：「3 张表」→「4 张表 = 3 实体 + 1 日志表 `mcp_tool_call_logs`」（以 03-data-model 为准）

### Step 2 — PR-01 端点冻结草案
`docs/specs/04-commands` 新增 **§2.6 MCP API**（8 端点）+ §三新增 4 条 MCP WS 事件。冻结时校正 IC-MCP 源头的 3 处全库口径漂移：
1. URL `/api/v1/mcp/...` → `/api/mcp/...`（全库无 `/v1/`，对齐 `/api/agents`）
2. WS `{"event":...}` 扁平 → `{"type":"tool_call:*","payload":{...}}` 信封 + `request_id`（AP-07）
3. 错误统一 `{error:{code:"E_MCP_*",message}}`（AP-02）
- 已在 `IC-MCP-V1.0-20260602.md` 顶部加 ERRATA 横幅指向 §2.6，避免旧格式再被照抄

## 验证
- `check_docs.py`：7 个失败均为**既有问题**（hooks 未装 + CLAUDE.md 中文路径编码校验），我改的 4 个文件无新增失败
- 全部 docs-only，未触碰 `src/` 任何代码

## 追加：计划整理 + 决策落地（同日第 2 轮）

按用户「先整理计划」+ 2 项决策（AskUserQuestion）执行：

### 决策
1. **URL 前缀 = `/api/mcp/`**（对齐现有 `/api/agents`），**AP-05（URL 版本号）暂缓** → 立 [ADR-0003](../decisions/0003-mcp-url-prefix-and-ap05-deferral.md)；同步在 `04-api` AP-05 行加现状注记。
2. **目录整理 = 移 `docs/archive/` 加 `DEPRECATED_` 前缀**。

### 整理动作
- 归档原计划残留 → `docs/archive/DEPRECATED_MCP接入-原计划残留/`：`07-文件框架/`（445 虚构桩）+ `bak/`（22）+ `02-重复变体/`（3 个 `*-MCP接入-*` 旧变体；权威 `*-MCP-*` 留原位）+ 归档 README。整个 `MCP接入/` 在 git 中本就未追踪（今天新建），整理是首次提交前清场。
- **文件结构图（FS-MCP）系统重写**：原 §1 树连现有结构都画错（`api/v1/`、`db/models/` 当包、`agentruntime/`、`application/mcp/`、`db/session.py`、`redis/`）→ 全部校正为真实结构（`api/routers/`、单文件 `db/models.py`、`infrastructure/llm/`、`domain/llm/protocol.py`、`application/services/mcp_*_service.py`、`infrastructure/mcp/dry_run.py`）。§3 各阶段表 + §6 注入链 + §7 同步修。
- **次级文档路径一致性**（单一权威原则，指向而非复制）：SA-MCP / closure-verdict / end-to-end-trace / MCP-UI-frontend 加 ERRATA 指针横幅（指向 FS-MCP §1 + 04-commands §2.6 + spec01 §MCP.1）；MCP-UI-frontend 的 `const API` 内联改 `/api/mcp`；TA-MCP / MD-MCP / spec03 的单点旧路径内联修正；IC-MCP 顶部已有 ERRATA。
- **追踪文件**：STATUS 袁行 + roadmap §十（加 P0 行 + PR-01 前置门 + 修正 ADR 编号冲突 0002/0003→0004~0007）+ FILE_GRAPH §5.3 改归档登记 + §5.6 新增本批记录。

## 给下一位的交接
- **§2.6 仍是「🔒 PR-01 冻结草案」**，未经 2 人 Review，**不算冻结**。P1 启动前必须：2 人 Review §2.6 + §三 MCP WS → Approve（PR-06）。
- Review 重点：URL 前缀 `/api/mcp/`（确认不要 `/v1/`，依据 ADR-0003）、WS 信封与 `request_id`、错误码清单、幂等键 `workspace_id+mcp_id+args_hash`。
- 落地权威三件套：文件结构 → `06-详细设计/FS-MCP §1`；接口 → `docs/specs/04-commands §2.6`；架构/数据 → `docs/specs/01-architecture §MCP` + `03-data-model §MCP`。次级文档（SA/TA/MD/UI/08）一律以这三处为准。
- Review 通过后方可进 P1：先 PR-09 确认 spec 同步 → 写 alembic 0006 + `infrastructure/db/models.py` 追加 4 表。
- `attach_mcp` 精确签名（是否随 `AgentRequest` 透传）留到 P2 实现前由 PR-09 冻结。
- 全程 docs-only，未碰 `src/`，未 push。
