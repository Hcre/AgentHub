# 2026-06-03 · MCP P1 核心链路落地 + 二次对账审计

> 作者：袁（xiangbianpangde）· 分支 `feature/mcp/pr01-freeze-and-plan-cleanup`

## 背景

用户更新了 MCP 接入计划，要求校验合理性、先收束计划组织、再开始实现。验证修订版（README-REVISION）对真实代码树属实后，用户作为 Reviewer 确认 §2.6/§三 契约（PR-01 闸门过），授权推进 P1。

## 做了什么

### 1. 二次对账（schema↔代码审计，R1-R10）
首轮可行性 review 是宏观路径审计（数 `src/agenthub/` vs `src/backend/app/` 引用），**没逐文件核对被引用实体是否存在**。P1 启动前补这一层，发现 10 项缺陷：

| 缺陷 | 真相 | 处置（Reviewer 确认） |
|------|------|------|
| R1 workspace_id FK→workspaces | 无 workspaces 表（workspace=session.workspace_path 字符串） | 裸 Uuid 无 FK，暂存 session_id |
| R2 created_by/installed_by FK→users | 无 users 表 | 裸 Uuid 无 FK，存 JWT sub |
| R3 JWT+成员校验 | 全端点零 JWT 强制，无 membership | 仅加 get_current_user 解析 |
| R4 "既有 trace_id" | 零出现 | 净新增，P4 生成 |
| R5 WS "信封对齐既有" | 既有扁平 {type,seq,content}，无 payload/request_id | 新信封并存，P4 决策 |
| R6 异步落盘 MQ | 无 MQ | Redis/后台任务，P4 |
| R9 错误体 {error:{code,message}} | 实际发 {detail}，全库违反 AP-02 | 沿用 {detail}，E_MCP_* 逻辑码 |
| R10 JSONB/TEXT[]/ENUM/BIGSERIAL/GIN | 测试走 SQLite create_all 必须可移植 | JSON/String/BigInteger.with_variant |
| R7/R8 | Redis/AgentRuntime/alembic/router 均在 | ✅ 通过 |

spec 已三处加对账横幅：`03-data-model §MCP`、`04-commands §2.6`、`README-REVISION §9`（单一权威）+ `MD-MCP` errata。

### 2. `.gitignore` 阻断 bug（严重）
裸 `backend/`（代码图谱段，第 63 行）误匹配 `src/backend/`，导致**所有新增后端文件被静默忽略**（已跟踪文件不受影响，所以一直没暴露）。仓库根无 `backend/` 目录 → 锚定为 `/backend/` 修正。不改这条，本期 P1 代码一行都 commit 不进去。

### 3. P1 核心链路代码（5 层洋葱）
- **L2 Domain**：`domain/mcp/{mcp_server,mcp_installation,mcp_binding,rules}.py`（dataclass 实体 + args_hash/version/batch 规则）+ `domain/repositories/mcp_repository.py`（2 ABC）+ `domain/enums.py` 追加 4 枚举
- **L1 Infra**：`models.py` 追加 4 表（可移植类型）+ alembic `0006-0009`（每表一迁移，幂等 guard + downgrade）+ `infrastructure/repositories/mcp_repository.py`
- **L3 App**：`mcp_market_service.py`（list/detail）+ `mcp_install_service.py`（幂等安装，P1 骨架直接 ready）
- **L4 API**：`api/routers/mcp.py`（`/api/mcp` 3 端点）+ `schemas/mcp.py`（Pydantic v2）+ `deps.py` 加 `get_current_user`（JWT 仅解析）+ 服务/仓储 DI + `main.py` 注册路由

### 4. 验证
- 新增 `tests/test_mcp.py` 12 用例（rules 4 + market 3 + install 4 + 路由注册 1），三路径覆盖（T-03）→ **12 绿**
- 全量 **95/98 通过**；3 失败为环境无关（pi-agent CLI 未装 / selector LLM 兜底，未碰这些文件）
- ruff lint 新代码全过；ruff format 已规整（5 个 lint error 在未触碰的 claude_code_process_pool.py）
- 本机补装 test 依赖：aiosqlite / fakeredis / pytest-cov / anthropic

## 给下一位的交接

1. **未 commit/push**（袁惯例等确认）。push 前务必含 `.gitignore` 修正，否则新增后端文件全丢。
2. **权威跑测在 Docker**：`docker compose exec backend pytest -q`。本机系统 Python 已可跑但非权威。
3. **P1 剩余**：DELETE `/api/mcp/installations/{id}` 卸载 · 真实安装进程+健康检查（替换骨架 ready）· `GET /api/mcp/market/templates`（官方 5 模板）。
4. **P2 起点**：`attach_mcp(...)` 扩展点（`domain/llm/protocol.py::AgentRuntime` + 3 个 runtime 实现）；绑定端点注意 `agent_mcp_bindings` 的 UNIQUE(agent_id, installation_id) 与软删冲突（见 models.py NOTE，需改部分唯一或硬删）。
5. **落地口径**全部记在 `README-REVISION §9` + 三处 spec 横幅，照那个写别照旧表。

## 红线对照
PR-01 ✅(Reviewer Approve) · PR-08 ✅(roadmap/STATUS 更新) · PR-09 ✅(spec 同步) · CR-03 ✅(alembic 续链) · T-03 ✅(三路径) · AR-01 ✅(洋葱分层，domain 不依赖 ORM) · AR-02 ✅(attach_mcp 走 Adapter，未另起运行时)
