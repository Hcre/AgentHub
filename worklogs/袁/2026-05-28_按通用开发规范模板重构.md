# 工作日志：按通用开发规范模板重构

- **谁**: 袁
- **日期**: 2026-05-28
- **分支**: chore/align-with-conventions
- **关联 Spec**: 全项目结构重组，参考 `c:/Users/yhn/Desktop/开发规范/` 模板

## 目标

把 AgentHub 的「文档 / 元信息 / 规范 / 工作流」层按通用开发规范模板对齐，**不动产品代码**。让以后任何 AI Agent 或新协作者按一致的目录决策树就能找到东西。

## 产出

11 个 commit（Phase 0 ~ 11），约 110 个文件变更，0 行业务代码改动（除 backend/app/domain/entities/group.py 一处 docstring）。

- [x] **Phase 0** 安全网：`chore/align-with-conventions` 分支 + 暂存所有 untracked
- [x] **Phase 1** 从内嵌 `开发规范/` 副本 cp 模板资产（35 文件：conventions 模板 + ai-workflow 7 篇 + docs/templates 15 个 + dashboard + 脚本）
- [x] **Phase 2** 根目录清理：删 `历史文档/`、`.gitignore` 追加运行时/缓存目录、`docs/*.html` 归入 `docs/reports/`
- [x] **Phase 3** 元信息层：写新 `meta/FILE_GRAPH.md`（按 AgentHub 实际结构）+ 根 `STATUS.md` + `dashboard.html` 启用
- [x] **Phase 4** **核心写作任务**：16 个规范文件
  - 6 个主规范（01 架构 + 02 代码 + 03 Git + 04 API + 05 测试 + 06 文档）用模板骨架填 AgentHub AR/CR/PR 红线
  - 2 个 AgentHub 特有附录（99-boundaries 边界矩阵 + 99-process-rules 流程红线全集）
  - 7 个 ai-workflow 流程文件（cp + 注入 AgentHub 实例化：verify.bat / code-review skill / pre-merge-check）
  - 3 个导航文件（CLAUDE-规范导航 + README-规范导航 + conventions/README.md）
- [x] **Phase 5** `spec/` → `docs/specs/`（10 文件 + domains/ 3 子文件 + roadmap 迁 `docs/plan/`）
- [x] **Phase 6** docs/ 重组：plan/research/templates 归位（7 mv），删空 `docs/规范文档/`
- [x] **Phase 7** **高风险阶段**：`.agenthub/worklogs/` → `worklogs/` + 改脚本路径常量（check_worklog 5 处、check_docs 3 处含错误信息字符串、feat-complete/feat-start/gen_worklog/code-review 等执行入口）；保留 `.agenthub/{config.json, skills/}` 不动（小红书 3 个 skill 与根 skills/ 完全不重叠）；新建 `worklogs/decisions/` 并迁入 `0001-cli-first-pivot.md`
- [x] **Phase 8** 全局引用更新（20 文件 PowerShell str.replace 批处理 + 个别手动修正）
- [x] **Phase 9** CLAUDE.md 按模板风格 8 节全文重写
- [x] **Phase 10** 验证：`check_docs.py` + `check_worklog.py` 跑通；修了 7 个 issues（含 3 个我引入的：CLAUDE 中的 glob 模式、worklogs/decisions/ 命名规则、docs/reports 中 .html）+ 4 个 pre-existing（前端实施计划_v1 重命名、3 个 group-chat 探索文件移入 `docs/explore/董/`）
- [x] **Phase 11** 删除内嵌 `开发规范/` 副本

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 不动 backend/frontend 代码内部组织 | backend/app 已是完美 5 层洋葱，改名收益为零，代价（imports + spec 文档全跟改 + 破坏所有 PR）极高 | 0 行业务代码改动，纯文档/规范/元信息层对齐 |
| worklogs 保留按人分子目录（违背模板的按日期平铺）| AgentHub 多人协作现实，便于追溯责任；模板没考虑多人场景 | check_worklog.py + STATUS Git↔人映射继续生效 |
| `.agenthub/{config.json, skills/}` 保留不删 | 实测确认与根 `skills/` 完全不重叠（小红书 dbs/xhs 系列），是项目专有运行时 | `.agenthub/` 残余但功能完整 |
| ADR 新位置 `worklogs/decisions/`（模板的位置）| 收束节点产出物归 worklogs/ 而非 docs/explore/，避免与 explore/EXP/个人探索混淆 | 0001-cli-first-pivot.md 编号制重命名 |
| check_docs.py 三处增强 | 适配新结构：`worklogs/decisions/` 跳过日期校验、ADR 命名校 NNNN-slug、`docs/reports/*.html` 允许（渲染产物存档） | 完整 backward compat + 新结构支持 |
| AR-02/04/05/AP-02 等 AgentHub 特有红线全保留 | 项目特有约束（新 Adapter 只加 / Agent 不直通 / FSM 事件溯源 / error 响应统一 `{error:{code,message}}` 与模板 `{code,message,data}` 不同）| 红线 27 条无损迁移（自动校验：AR-01/CR/PR/AP/T/D 全部命中目标位置）|
| 删 `spec/rules/` 后 `arch-rules` 和 `code-rules` 归档 `docs/archive/DEPRECATED_*` | 内容已并入 conventions/01-02，但保留历史方便追溯 | archive/ 多 2 文件 |

## 未完成 / 阻塞

- **pre-commit hooks 未在本地装机**：`scripts/check_docs.py` 报 D-12，但这是环境设置问题。需协作者各自跑 `pre-commit install --hook-type pre-push && pre-commit install`
- **frontend/ 团队特有目录命名**（`frontend/问题/`）未规范化为英文，保留现状
- **STATUS.md「Git ↔ 目录映射」缺 董 的 Git 用户名**（标"待补充"），董首次 push 时需补
- **dashboard.html 视觉验证未跑**（需启 `scripts/start_server.py` 在浏览器看，留待协作者各自验证）

## 给下一位的交接

> **此 PR 合并后**，所有协作者需做 3 件事：
>
> 1. **rebase 自己的 feature 分支到新 main**：spec/ 路径全死，`.agenthub/worklogs/` 路径全死。`git rebase main` 时大概率有 conflict，参考 `Phase 8 commit message` 的替换规则表逐项对齐。
> 2. **首次拉新 main 后必装钩子**：`pre-commit install --hook-type pre-push && pre-commit install`，否则 push 全挂。
> 3. **新规范快速适应**：
>    - 红线/规则去 `conventions/` 而非 `spec/rules/`
>    - 规格去 `docs/specs/` 而非 `spec/`
>    - 路线图去 `docs/plan/开发清单_roadmap.md` 而非 `spec/roadmap_*`
>    - STATUS.md 在根（不在 `.agenthub/worklogs/`）
>    - 新增文件先查 `meta/FILE_GRAPH.md §三 决策树`

参考：
- 完整计划：`C:/Users/yhn/.claude/plans/shimmering-prancing-tarjan.md`
- 教训来源：`c:/Users/yhn/Desktop/research-tool/meta/AI重构笔记.md`
- 模板冷源：`c:/Users/yhn/Desktop/开发规范/`（项目内 `开发规范/` 副本已在 Phase 11 删除）
