# t1-pin-auth 任务 worklog — 2026-06-08

## 任务背景
plan_3eaba0fa t1-pin-auth: 修 Pin API 401 bug + alembic 0014 merge + 401/403/204 三路径测试
- 22:00 E2E (commit 0ebe3b2) 发现前端 hover → Pin → console 401
- root cause: `get_current_user` 在前端不发 Authorization header 时返 None → endpoint 主动 401
- M5 方案: endpoint 不强 401, service 层 auto-trust via msg.user_id

## 完成

### 1. 排查 root cause
- 读 `src/backend/app/api/dependencies/auth.py` (后) + `src/backend/app/api/deps.py:65-78` (实际位置) + `sessions.py:87-122` + `session_service.py:89-127`
- 确认: endpoint `if current_user is None: raise HTTPException(401)` 是 bug 来源

### 2. M5 鉴权降级契约 (per docs/specs/04-commands §6.1.6 B-1-P0-04)
- 有 JWT → 强制 current_user == msg.user_id (否则 403 E_MESSAGE_PIN_NOT_OWNER)
- 无 JWT + msg 有 user_id → auto-trust 用 msg.user_id (204, dev mode)
- 无 JWT + msg 无 user_id (system message) → 401 E_AUTH_REQUIRED
- session mismatch → 422 (不变)

### 3. 改动文件
- **src/backend/app/api/routers/sessions.py**: pin/unpin 移除硬 401 check
- **src/backend/app/application/services/session_service.py**: pin_message / unpin_message 改 M5 鉴权降级
- **src/backend/app/core/exceptions.py**: 新增 `AuthRequiredError(PermissionError)` 用于 401 区分
- **src/backend/app/main.py**: 新增 AuthRequiredError → 401 handler
- **src/backend/tests/test_pin_session_ownership.py**: test_pin_route_anonymous_401 → test_pin_route_anonymous_404
- **src/backend/tests/test_pin_auth.py** (新增): 12 测试

### 4. 3 commits push origin/feature/m5/pin-auth-fix
- **b97c4fd** `fix(backend): Pin API 401 bug 修复 — M5 鉴权降级契约` (4 files, +63/-19)
- **bd92b2a** `docs(specs): 04-commands §6.1.6 复写 M5 鉴权降级 — When-5/6/7 覆盖 401/403/204` (1 file, +8/-1)
- **5371f41** `test(backend): pin auth 401/403/204 三路径 pytest + 22:00 E2E 401 bug regression` (2 files, +465/-5)

### 5. pytest 169/171 绿
- 12 新 test_pin_auth.py 全绿
- 5/5 test_pin_session_ownership.py 全绿 (含改名 1 个)
- 152 原测试不变
- 2 失败 deferred: test_pi_agent_e2e.py (本机无 pi binary 已知, 非本任务 regression)

### 6. alembic 0014 merge 验证
- `alembic heads` → 0014 (head) (mergepoint) ✓
- `alembic upgrade head` ✓
- `alembic downgrade 0011` → 0011 (branchpoint) ✓
- `alembic upgrade head` → 0014 (head) ✓
- merge migration 已在 d6c503b (plan_ba86c4d0 强收时落), 本任务仅验证 + 引用

## 未完成 (70min timeout kill)

### Playwright E2E 验证 + 截图
- 启动 isolated uvicorn 在 18010 (worktree 路径, 含 M5 修复代码)
- uvicorn 启动成功 (Test-NetConnection 18010 = True)
- **未及**: 用 Playwright MCP 连 18010 跑 Pin 真 E2E
- 阻塞原因: uvicorn 启动后立即到 70min cap, 没时间做完整 Playwright run

### Worklog + deliverable.md
- 本 worklog 写于 retry 阶段
- deliverable.md 待写 (见 plan workspace)

## 阻塞根因
1. **多 worker git race** (重蹈 t2-token-monitor 覆辙): 第一次 checkout feature/m5/pin-auth-fix 时被其他 worker 切换到不同分支, 编辑丢失. 通过 `git reflog` + 重建 worktree 解决.
2. **isolated worktree 创建浪费 5min**: 第一次不知道要 worktree, 直接在主 worktree 编辑导致 race; 第二次创建 isolated worktree `C:\Users\yhn\Desktop\字节比赛\agenthub_pin_auth` 才稳定.
3. **HTTP test fixture 复杂**: client_with_db 需要复制 main.py 的 5 个异常 handler + 3 个依赖 override. 第一版漏 handler → PermissionError 变 500; 加 handler 后全绿.
4. **AuthRequiredError 引入迭代 2 次**: 第一版用 PermissionError 区别消息前缀, 但 PermissionError handler 一律 403; 必须新增 AuthRequiredError 子类 + 401 handler.
5. **测试数 12 比 brief 3 多 9**: brief 写 +11 测, 我做了 12. 12 测试需要 4-5 个文件编辑/调试迭代.

## 给下一个 worker 的建议
1. **直接进 isolated worktree** `C:\Users\yhn\Desktop\字节比赛\agenthub_pin_auth` 工作
2. **3 个 commit 已在 origin/feature/m5/pin-auth-fix**, 验证 + merge main 即可
3. **Playwright 跑前重启 docker backend** 用新代码, 或用 isolated worktree 启动 uvicorn :18010
4. **不要重复** Pin 修复 — 代码已 work, 169/171 绿
5. **40min cap 严守**: 12 测试 + 4 commit 已够, 别再加测试或文件

## 关键 commit
```
b97c4fd fix(backend): Pin API 401 bug 修复 — M5 鉴权降级契约
bd92b2a docs(specs): 04-commands §6.1.6 复写 M5 鉴权降级 — When-5/6/7 覆盖 401/403/204
5371f41 test(backend): pin auth 401/403/204 三路径 pytest + 22:00 E2E 401 bug regression
```

## 关键文件路径
- isolated worktree: `C:\Users\yhn\Desktop\字节比赛\agenthub_pin_auth\`
- feature branch: `feature/m5/pin-auth-fix` (本地 + origin)
- 修复主文件: `src/backend/app/{api/routers/sessions.py, application/services/session_service.py, core/exceptions.py, main.py}`
- 新测试: `src/backend/tests/test_pin_auth.py` (12 测试)
- spec 更新: `docs/specs/04-commands_命令接口.md` v2.2 → v2.3
