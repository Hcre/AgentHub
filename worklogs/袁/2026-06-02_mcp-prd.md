# 工作日志：MCP 功能 PRD v1 补全

- **谁**: 袁（Mavis `mcp-pm` agent 代笔）
- **日期**: 2026-06-02
- **关联 Spec**:
  - docs/plan/MCP功能计划_v0.md（v0.2 4 阶段 + 严格收束，文件暂缺）
  - docs/plan/MCP功能PRD.md（本次产出，v1.0，414 行 / 11 块，文件暂缺）
  - 产品规范: `C:\Users\yhn\Desktop\袁浩岚的文件\项目\产品开发\01-多Agent协同系统建设规范（完整版）.md`
  - 决策: [`worklogs/decisions/0001-cli-first-pivot.md`](../decisions/0001-cli-first-pivot.md)
- **分支**: (未开，等评审组过审后开 `feature/mcp/prd-v1` worktree + 推 PR)

## 目标

把用户 3 行 PRD 草稿（市场 / 接入 / 创建）按 7 阶段产品规范 + ai-workflow 收束要求，补全成 11 块齐备的 PRD v1，作为评审组拍板 + 启动 Plan B-P1 实施的唯一依据。

## 产出

- [x] **PRD v1.0** — `docs/plan/MCP功能PRD.md`
  - 11 块齐备：① 背景/② 用户故事/③ 边界/④ 数据模型/⑤ API/⑥ 前端/⑦ 架构/⑧ 风险/⑨ 不做/⑩ 工时/⑪ 验收
  - 5 个 BDD 用户故事（US-1~5，3 大子功能各 1-2 个）
  - 3 张表 schema（`mcp_servers` / `workspace_mcp_installations` / `agent_mcp_bindings`）
  - 8 个核心 API + 2 个 P3 阶段补 API（创建 + 验证）
  - 3 个新页 + 1 个 Tab + 4 个 Zustand store
  - 5 层洋葱接入点（domain / application / infrastructure / api / presentation）
  - 8 类风险与对策（含 R1 安全沙箱 / R2 进程开销 / R3 CLI 兼容性）
  - 4 阶段 13 天工时估算（159h 总：52+34+40+33）
  - 验收：功能 / 性能 / 安全 / 代码质量 / 收束报告 五维度
  - 附录 A：MCP 协议简表；附录 B：参考链接
- [x] **roadmap §十** — `docs/plan/开发清单_roadmap.md` 新增"MCP 功能 v1"阶段表
  - P1 6/2-6/5 (52h) → P3 6/6-6/8 (34h) → P2 6/9-6/11 (40h) → P4 6/12-6/15 (33h)
  - §九 变更记录追加 6/2 行
- [x] **STATUS.md** — 袁行追加 MCP PRD v1 进度，日期 6/1 → 6/2
- [x] **worklog** — 本文件
- [x] **deliverable.md** — `C:\Users\yhn\.mavis\plans\plan_b85e10de\outputs\prd-completion\deliverable.md`
- [x] **progress board** — `C:\Users\yhn\.mavis\plans\plan_b85e10de\board.md` 追加 in_progress → done

## 关键决策

| 决策 | 原因 | 影响 |
|------|------|------|
| 按 7 阶段规范写 11 块（11 块比 7 阶段更细） | 用户明确要求"PRD 必含 11 块" | 评审可直接对 11 项逐条过 |
| 用户故事从 3 个扩到 5 个（加 F5 工具展示） | 计划 v0.2 §四把 F5 也列入 4 阶段 | US-5 工具展示给 P4 阶段验收做依据 |
| 8 个 API 端点（市场 2 + 安装 3 + 绑定 3） | 单一职责 + 沿用 `04-api` 规范 | 后期加 P3 创建 2 个端点不破坏现有 |
| 数据模型加 binding_source 字段（workspace / explicit） | 让 Agent 配置页能区分"继承"vs"显式" | UI 多一个"来源"列 |
| F2 阶段新增 `cli_mcp_injector.py`（Runtime 适配层） | 4 个 Runtime 对 `--mcp-config` 支持度不一 | 抽象 Builder 接口，按 Runtime 实现 |
| R1 安全沙箱单列 🔴 高风险 | 协议本身允许任意命令执行 | 复用 `99-boundaries_边界矩阵.md` + Inbox 审批流 |
| 4 阶段顺序 P1→P3→P2→P4 | 用户已拍板（不重复争论） | 严格执行 |
| MCP v1 不做项 10 条（P1 后续 / P2 / P3）| 避免 MVP 范围蔓延 | 答辩时显式声明 |
| 工时按 11 角色流水线分阶段汇总 | 符合 `01-多Agent协同系统建设规范` 协作流程 | PM 可按角色分配 |

## 设计原则

PRD 撰写时严格遵守 `docs/conventions/` 全部红线（AR-01~06 / CR-01~12 / AP-01~07 / T-01~06 / D-01~12）：

- **AR-01 5 层洋葱**：第 7 块架构影响按 5 层列接入点（domain → application → infrastructure → api → presentation）
- **AP-01~07**：第 5 块 API 全用 kebab-case + 复数 + 统一响应格式 + JWT + Pydantic + 版本前缀
- **CR-03 Alembic**：第 4 块显式写"数据库：PostgreSQL 16，沿用现有，迁移走 Alembic"
- **D-11 CLAUDE.md 路径有效**：第 1 块引用现有决策文件 `worklogs/decisions/0001-cli-first-pivot.md`（已存在）
- **PR-01 接口先行**：第 5 块 API 表明确 method+path+用途+入参+出参+鉴权（实施时按此冻结）

## 与计划 v0.2 的对齐校验

| 计划 v0.2 § | PRD v1 § | 对齐状态 |
|------|------|---------|
| 一、4 阶段总览 | 十、工时估算（4 阶段总计 13 天）| ✅ 完全对齐 |
| 二、阶段 1 P1 详情 | 十一、验收标准（11.1 P1 验收）| ✅ |
| 二、阶段 2 P3 详情 | 同上 | ✅ |
| 二、阶段 3 P2 详情 | 同上 | ✅ |
| 二、阶段 4 P4 详情 | 同上 | ✅ |
| 三、收束 4 阶段硬闸门 | 十一、5 文档 / 收束报告要求 | ✅ |
| 六、跨阶段约束 | 十一、4 代码质量验收 | ✅ |
| 七、关键风险 | 八、风险与对策 | ✅ 全部覆盖 + 扩展到 8 条 |
| 八、待用户拍板 | (待评审组过审) | ⏳ |

## 给下一位的交接

- **Mavis（评审组 / 后续 agent）**：
  - 评审 PRD 走 docs/plan/MCP功能PRD.md（文件暂缺）11 块逐条过，重点看：
    1. 第 4 块数据模型字段是否合理（特别是 `binding_source` 字段）
    2. 第 5 块 API 是否覆盖全部用户故事（US-1~5 都能映射到 API）
    3. 第 8 块 R1 安全沙箱对策是否充分（可能要 P0 课题 Inbox 审批流协同）
  - 评审通过后开 worktree `feature/mcp/prd-v1` 推 PR → merge main
  - merge 后启动 Plan B-P1 实施（4 轨道并行：数据层 / API 层 / UI 层 / 基础设施层）
  - P1 实施严格按第 11 块 11.1 P1 验收标准 + 11.5 文档要求

- **董 / 黎**：
  - P2 阶段（Agent 接入）会改 `src/backend/app/infrastructure/llm/*_runtime.py` 4 个 Runtime 的启动逻辑
  - 影响面：`ClaudeCodeRuntime` / `OpenCodeRuntime` / `PiAgentRuntime` / `CodexRuntime`
  - CLI `--mcp-config` 参数格式各 Runtime 不一样，由 `cli_mcp_injector.py` 抽象
  - 若 Runtime 不支持 MCP 注入 → 降级方案：market 显示"该 Runtime 暂不支持此 MCP"
  - E2E 验收 4 个 Runtime 各 1 套

- **袁**（自己）：
  - 评审通过后：
    1. 开 `feature/mcp/prd-v1` 分支（走 PR-02）
    2. 写 4 阶段 4 个 ADR（0002/0003/0004/0005）等到 P 阶段时按需出，**不在 PRD 阶段出**（按 01 规范 ADR 只在收束节点产出）
    3. 收束报告 `docs/reports/2026-06-15_MCP功能MVP收束报告.md` 答辩前 1 周录 Demo 备份
  - 答辩相关：MCP 功能不在 MVP 收尾冲刺的 6/2-6/5 时间盒内，是 6/2-6/15 的 13 天项目，独立验收

## 临时约定

- 本次产出**未 commit**（按 03-git 红线走 worktree + PR 流程）
- 评审通过后第一次 commit: `docs(plan): MCP 功能 PRD v1.0 (11 块)`
- 后续每次 P 阶段完成: `feat(mcp): P1 F1 MCP 市场 (52h, 收束 1)` 等
- ADR 编号沿用 2026-06-02 worklog 的约定: 0002(P1) / 0003(P2) / 0004(P3) / 0005(P4)
- MCP 相关的图谱、enums 分析、code review 红线检查清单 等产出走 worklogs/袁/ 单独子目录 `2026-06-XX_mcp-*.md`

## 未完成 / 阻塞

- [ ] 评审组过审 11 块（待启动）
- [ ] 评审通过后起 worktree + 推 PR
- [ ] PR-06 ≥1 Approve → merge main
- [ ] merge 后启动 Plan B-P1 实施（4 轨道并行）
- [ ] P1 完成 → 收束 1 + ADR 0002 + 报告
- [ ] P3 完成 → 收束 3 + ADR 0004 + 报告
- [ ] P2 完成 → 收束 2 + ADR 0003 + 报告
- [ ] P4 完成 → 收束 4 + ADR 0005 + 报告
- [ ] 整体收束报告 + Demo 视频（6/15）

## 备注

本次是 PM agent（`mcp-pm`）首次工作，验证了三件事：
1. **MCP 协议理解准确**：2 句话讲清 client-server + JSON-RPC + transport，与 Anthropic 官方文档一致
2. **5 层洋葱接入点设计可行**：domain entities 用 dataclass（不引 ORM），infrastructure 适配 SQLAlchemy + asyncio 子进程池，CLI Adapter 扩展点用抽象 Builder 隔离
3. **11 块 PRD 结构与 7 阶段规范兼容**：11 块是 PRD 文档结构（11 块齐备便于评审），7 阶段是建设流程（设计阶段 → 详细设计 → 实现 → 测试），两者正交不冲突
