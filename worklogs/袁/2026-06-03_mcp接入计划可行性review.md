# 2026-06-03 · MCP 接入计划可行性 Review

## 做了什么
- 通读 `docs/plan/后续升级计划/MCP接入/`（01-需求澄清 ~ 08-系统模拟运行 全套 8 棒 + 22 模块框架）。
- 对照真实代码库 `src/backend/app/`、CLAUDE.md（AR-01~06）、roadmap §十，做可行性判定。
- 产出《可行性问题清单》：`docs/plan/后续升级计划/MCP接入/可行性问题清单_2026-06-03.md`（12 条问题 + 复用清单 + 修订范围 + 4 项待决策）。

## 核心结论
- **产品目标可行，工程方案不可按原样落地**。
- 阻断项：① 计划落进虚构的 `src/agenthub/` 新仓（69 文件引用，真实树不存在），与现有洋葱冲突；② 分层方向与 AR-01 相反；③ 违反 AR-02（另起运行时而非加 Adapter）；④ 技术栈漂移（Poetry/gRPC/Vault/OTel/K8s）；⑤ ~13/22 模块为与 MCP 正交的企业级基础设施，MVP 期严重超纲。
- 08 的"三层闭环🟢收敛"仅在计划虚构的系统内成立，对真实代码库不成立。

## 决策
- 用户（袁）选择：**先列问题清单，自行修订计划后再启动开发**。本次不写任何产品代码。

## 给下一位的交接
- 修订计划时以《可行性问题清单》§4 建议范围为目标：落在 `src/backend/app/` 5 层，对齐 roadmap §十（3 表 / 8+2 端点 / 3 页+1 Tab / 复用 CLI Adapter），安全基础设施全部移下期。
- 需先定 §5 的 4 项开口（单一权威 / 表名口径 / dry-run 是否本期 / SDK Adapter 是否本期）。
- 实现启动前走 PR-01（`docs/specs/04-commands` 端点冻结）+ CR-03/PR-09（`docs/specs/03-data-model` + alembic 续 0006）。
- PRD 需求口径可直接复用 `01-需求澄清/PRD-MCP-V1.3`；07 桩代码不要直接拷贝。
