# 工作日志：目录二次整合 + 引用修复

- **谁**: 袁
- **日期**: 2026-05-29
- **分支**: chore/align-with-conventions

## 目标

用户手动做了第二轮目录整合，但只搬了文件、没改引用，且误删了 conventions/。修复整条引用链，让重组后的结构可用。

## 整合后的新结构

| 原 | 新 |
|---|---|
| `backend/` `frontend/` `docker/` | `src/{backend,frontend,docker}/` |
| `conventions/` | `docs/conventions/` |
| `skills/` | `docs/skills/` |
| `.agenthub/` | `docs/.agenthub/` |

## 产出

- [x] **conventions/ 误删恢复**：`git checkout HEAD -- conventions/`（19 文件）→ `git mv` 到 `docs/conventions/`
- [x] **git rename 识别**：`git add -A` 让 git 识别 252 个重命名（而非删除+新增）
- [x] **R1 关键配置**：
  - `.pre-commit-config.yaml`：`^backend/` → `^src/backend/`、`^frontend/` → `^src/frontend/`（4 处 files: + config 路径）
  - `src/docker/docker-compose.yml`：`../.agenthub/skills` → `../../docs/.agenthub/skills`、`../.env` → `../../.env`（2 个 service）；`../backend` `../frontend` context 不变（compose 在 src/docker，相对 src/ 仍对）
  - `scripts/{verify,code-review,deploy,feat-start,feat-complete,start-backend,start-frontend}.{sh,bat}`：cd 路径 + compose 路径前缀
- [x] **R2 入口文档树重写**：meta/FILE_GRAPH.md（整体重写）+ CLAUDE.md + README.md 目录树和引用表（树是结构性的，手改避免缩进错乱）
- [x] **R3 批量引用更新**：guarded 正则（负向 lookbehind 防双前缀）刷 ~45 文件：specs/design/explore/plan/.agenthub-skills/dashboard + docs/conventions 内部相对链接（`../docs/specs/` → `../specs/`、`../meta/` → `../../meta/` 等深度修正）
- [x] **R4 check_docs.py**：section 5 HTML 检查排除 `docs/skills/` `docs/.agenthub/`（Skill 资产可含 html 模板）
- [x] **R5 验证**：check_docs 仅剩 hooks 未装环境项；check_worklog 通过；docker-compose 4 个相对路径全部解析正确；无 `src/src/` `docs/docs/` 双前缀

## 关键决策

| 决策 | 原因 |
|------|------|
| docs/templates/ 排除批量替换 | 模板是给新项目复制的通用结构，应保持标准布局（conventions/ 在根），不反映 AgentHub 的 src//docs/conventions 特化 |
| 目录树用相对子节点 | CLAUDE/README/FILE_GRAPH 的 ascii 树中，`src/` 父节点下写 `backend/`（非 `src/backend/`）；批量正则误加前缀后手动改回 |
| docs/conventions 相对链接分深度处理 | 顶层 `conventions/*.md`（docs/conventions/）与子目录 `ai-workflow/*.md`（docs/conventions/ai-workflow/）相对深度不同，分别处理 up-link |

## 未完成 / 阻塞 / 风险

- ⚠️ **Claude Code Skill 发现机制**：`docs/skills/` 与 `docs/.agenthub/skills/` 不在 Claude Code 默认扫描路径（根 `skills/` 或 `.claude/skills/`）。整合后这些 skill 可能不再被自动加载——如需保持 skill 可用，建议把 skills/ 移回根或建软链。本次只修了文档引用，未验证 skill 运行时可发现性。
- **pre-commit hooks 未装**：环境项，协作者各自 `pre-commit install --hook-type pre-push && pre-commit install`
- **docs/00-overview 等旧有残留**：`assumptions_`、`rules/`、`PRD_v4` 等是整合前就存在的文档债，不在本次范围
- **start-backend.sh MAIN_VENV**：硬编码他人 worktree 绝对路径，未动

## 给下一位的交接

> 新结构：产品代码全在 `src/`，协作文档（含规范/skills/运行时配置）全在 `docs/`。
> - 找规范 → `docs/conventions/`
> - 跑 docker → `cd src/docker && docker compose up`
> - 本地校验 → `scripts/verify.sh`（已指向 src/backend、src/frontend）
> - ⚠️ 若发现 skill 不生效，是 docs/skills/ 不在 Claude Code 扫描路径所致，需评估移回根。
