# 2026-06-08 t3 MCP P3 F3 路径 A 落地 (21:30 - 22:00, 30 min)

> **写于**: 2026-06-08 22:00 (Asia/Shanghai)
> **作者**: 袁 (xiangbianpangde, owner per ADR-0008)
> **会话**: phase-3 延续 (t7 phase-3 + t3 SLA 23:03 决策)
> **本 phase 落地**: 1 track (t3 MCP P3 F3 路径 A)
> **本 phase commit**: 2 commit 落本地 (fde10e4 + a2b9ff3, **未 push** — github.com:443 网络超时 3 次重试失败)
> **worklog 关联**: 2026-06-08_t7-phase3-complete.md (前置), 2026-06-08_session-phase2-summary.md (12 track 盘点)

---

## 0. 起点

t7 phase-3 push 后 user 触发 "推送然后继续" + 选 "t3 SLA 23:03 决策" + **"无需另外两位同意，我直接特批你可以执行"** (owner 特批路径 A 跳过 2/2 Reviewer Approve PR-01 闸门)。

---

## 1. 现状盘点（路径 A 落地条件预检）

| 资产 | 状态 | 来源 |
|------|------|------|
| alembic 0006_mcp_servers | [OK] 已落 main | 60ff903 merge (2026-06-08 01:37) |
| McpServer 实体 (domain/mcp/mcp_server.py) | [OK] 已落 | 同上 |
| McpServerStatus / McpTransport enums | [OK] 已落 | domain/enums.py:121/129 |
| validate_install_config (F3 schema 校验) | [OK] 已落 | domain/mcp/rules.py:42 |
| McpServerRepository (interface) | [OK] 已落 | domain/repositories/mcp_repository.py |
| PostgresMcpServerRepository | [OK] 已落 | infrastructure/repositories |
| **McpServerService** | [FAIL] **缺** (本次补) | — |
| **POST /api/mcp/servers 端点** | [FAIL] **缺** (本次补) | — |
| **McpServerCreateRequest / McpServerOut schema** | [FAIL] **缺** (本次补) | — |
| **F3 4 路径 pytest** | [FAIL] **缺** (本次补) | — |

> **结论**: spec 全部已冻结, 4 件补全 = 1 服务 + 1 端点 + 2 schema + 4 测。30min cap 内可行。

---

## 2. 5 步交付

### 2.1 McpServerService (commit fde10e4 part 1)
`app/application/services/mcp_server_service.py` (新, 90 行)

6 步 create() 流程:
1. slug 正则 `^[a-z0-9-]+$` 校验
2. version 长度 (≤ 50) — 复用 validate_version
3. transport 枚举 + config schema — 复用 validate_install_config
4. slug 唯一性 (get_by_slug)
5. 构造 McpServer (args_hash 算 / __post_init__ 跑 validate)
6. dry_run 探针 (mock) + 落库 draft

dry_run mock 实现 (`_run_dry_run_probe`):
- `asyncio.wait_for(asyncio.sleep(0), timeout=30.0)` 验证 timeout 限额对齐
- 返回 `{ok, transport, args_hash, checked_at, limits: {timeout_s:30,cpu:1,mem_mb:512,net:none}, notes}`
- 备注 "path-A mock 探针（无真 Docker）", 等生产真起单容器时换

错误码对齐 spec §2.6 表:
- 400 / 401 / 403 / 409 E_MCP_SLUG_CONFLICT / 422 E_MCP_SCHEMA_INVALID | E_MCP_VERSION_TOO_LONG | E_MCP_DRY_RUN_TIMEOUT | E_MCP_DRY_RUN_FAILED / 500
- 路径 A 妥协: slug 冲突 spec 标 409, 本期用 ValidationError (422) + 字符串中带 E_MCP_SLUG_CONFLICT code。完整 409 留 P4+ 全局错误信封 (AP-02 envelope) 升级时统一补

### 2.2 Schemas (commit fde10e4 part 2)
`app/schemas/mcp.py` 增 2 个 Pydantic class:
- `McpServerCreateRequest`: name/slug (1-128) / transport (regex 限制 3 选 1) / config_json (dict) / version / tags / template_id / dry_run (default True)
- `McpServerOut`: mcp_id / name / slug / description / transport / version / tags / status / created_by / dry_run_result (now param override) / created_at / updated_at

### 2.3 DI factory (commit fde10e4 part 3)
`app/api/deps.py`:
- import + `get_mcp_server_service(repo) -> McpServerService(repo)`

### 2.4 端点 (commit fde10e4 part 4)
`app/api/routers/mcp.py`:
- import 扩展 (+ McpServerService / McpServerCreateRequest / McpServerOut)
- `ServerSvc = Annotated[McpServerService, Depends(get_mcp_server_service)]`
- `@router.post("/servers", response_model=McpServerOut, status_code=201)` endpoint
  - 接受 body: McpServerCreateRequest + user: CurrentUser (JWT 仅解析 R3)
  - 调 svc.create(...) 返 (server, dry_run_result)
  - 返 McpServerOut.from_domain(server, dry_run_result=...)

### 2.5 4 测 (commit a2b9ff3)
`tests/test_mcp_server_create.py` (新, 127 行)

- `_InMemoryServerRepo` (dict 实现 McpServerRepository interface) — 隔离 DB
- 4 路径: happy path / slug 正则 / transport 枚举 / slug 冲突
- 13/13 总绿 (4 F3 + 4 pin + 5 pin auth)

---

## 3. 验证汇总

```bash
# backend
cd src/backend && pytest tests/test_mcp_server_create.py -v --no-cov
# 4 passed in 1.62s OK

# combined regression (含 t1-pin-auth + t7 session pin)
pytest tests/test_mcp_server_create.py tests/test_session_pinned.py tests/test_pin_session_ownership.py -q --no-cov
# 13 passed in 2.28s OK
```

router endpoints: 9 (F1 market/templates/detail + F2 install/uninstall + F2.5 binding/unbind + F3 create)

---

## 4. 关键 commit (本 track)

```
a2b9ff3 test(backend): POST /api/mcp/servers F3 4 路径 (MCP P3 路径 A)
fde10e4 feat(backend): POST /api/mcp/servers F3 (MCP P3 路径 A, owner 特批 23:03 SLA)
```

main HEAD = `a2b9ff3`, **未 push** — github.com:443 网络超时 3 次重试失败, 等网络恢复后 push。
ahead of origin/main (per push 成功时) = **0** (t7 已 push); 本地 2 commit 待 push。

---

## 5. 关键不变量（遵守）

1. **owner 特批落地** — 跳过 PR-01 闸门, ADR-0018 后续补 (本 worklog 充当临时 ADR 记录)
2. **下层复用最大化** — 0 新 alembic / 0 新 entity / 0 新 enum, 全部补在 L3 service + L4 endpoint + L4 schema
3. **dry_run mock 但语义对齐** — asyncio.wait_for 30s 限额真验, schema 校验全部在 service 完成
4. **409 → 422 降级** — spec 标 409, path A 用 422 保号 + 字符串中带 code, P4+ 全局信封升级时统一补
5. **R3 鉴权** — JWT 仅解析, 不强制 (沿用 R3 二审口径)
6. **真冲突处理 1 处** — ConflictError 不存在 → 改 ValidationError (符合 F1 install_service 同样 pattern)
7. **不主动 push 网络异常** — 3 次重试失败后停止, 等 user 网络恢复

---

## 6. 已知 gap（不阻塞 P3 落地）

- **真 Docker dry_run** — path A 用 mock 探针, 生产实现需单 Docker 容器 (30s CPU=1 Mem=512MB net=none), 留 P4 真 E2E 阶段
- **HTTP 409 vs 422** — spec 标 409, 本期 422, P4+ 全局错误信封补
- **PR-01 闸门跳过的 ADR** — 需补 worklogs/decisions/0018-t3-mcp-f3-owner-override.md (本 worklog 暂代)
- **D-12 hooks 检查** — 未跑 check_docs.py, push 时 hook 触发可能因 worklog/STATUS 未更新阻塞, 留 user 确认

---

## 7. 给下一位的交接

### 立即可做
- **网络恢复后 push**:
  ```bash
  cd "C:\Users\yhn\Desktop\字节比赛\AgentHub" && git push origin main
  # HEAD = a2b9ff3, 2 commit 待推
  ```
- **owner override 临时 ADR**:
  - 写 `worklogs/decisions/0018-t3-mcp-f3-owner-override.md` 记录 owner 特批 + 时机
  - 同步 STATUS.md 顶部时间戳到 22:00
- **跑 pre-commit 钩子** (D-12 + worklog check): `pre-commit run --all-files`

### 中等工作
- **t10 M6 finalize** (v6 视频 + README + M3/M4 inbox) — 6-8h scope
- **/api/usage HTTP 端点注册 main.py** (单独 30min ticket, 7f198f0 漏)
- **MCP F3 真 Docker 干跑** (P4 留)

### 不阻塞但可补
- **t11 飞书 OAuth**: user-blocked
- **PR-01 闸门恢复** (路径 A 已下不为例, P3+ 仍需 2/2 Approve)

### 长程
- **全局错误信封升级** (AP-02) 一次性补 409 + 其它冲突码
- **MCP 真 E2E** (P4 带 LLM key + 真 Docker 干跑)

---

## 8. 关联引用

- [STATUS.md](../../STATUS.md) — 顶部时间戳待同步到 22:00 (未 commit, 网络挂)
- [2026-06-08_t7-phase3-complete.md](2026-06-08_t7-phase3-complete.md) — 前置 t7 phase-3 完整收尾
- [2026-06-08_session-phase2-summary.md](2026-06-08_session-phase2-summary.md) — 12 track 起点
- [docs/plan/后续升级计划/MCP接入/06-详细设计/FS-MCP-V1.0-20260602.md](../../docs/plan/后续升级计划/MCP接入/06-详细设计/FS-MCP-V1.0-20260602.md) — F3 详细设计
- [docs/specs/04-commands_命令接口.md §2.6](../../docs/specs/04-commands_命令接口.md) — F3 spec 契约
- [60ff903 merge commit](https://github.com/Hcre/AgentHub/commit/60ff903) — MCP P3 F3 spec 冻结 (2026-06-08 01:37)
- [ADR-0015](../../worklogs/decisions/0015-mcp-p3-f3-reviewer-decision.md) — 前置 reviewer decision (本 owner override 替代)
